# FENGARDE — Single Source of Truth

**This file is the canonical status/roadmap pointer for BOTH repos:**
- `fengarde` (this repo, public, Apache-2.0) — the shipped SIEM
- `fengarde-sec` (private, closed) — the proprietary LLM layer, see its own `docs/STATUS.md`

Every other doc below is historical detail, not competing truth. If a doc disagrees
with this file, **this file wins** — go fix the doc, don't trust it standalone.
Update this file whenever status changes; it's a living index, not an archive.

---

## 1. Current state (as of 2026-08-07, PR#55 merged to `main` — closed the public-repo code-task backlog handed to a second model, picked up mid-stream and finished in-session, then independently reviewed twice (cavecrew-reviewer, then a deeper pass whose fixes landed as a follow-up commit) before merge — on top of PR#54 [2026-08-06]. See the new row directly below for the full account, and §5 for the milestone-by-milestone breakdown — this header date is the floor for everything in this table, not every individual row's own date)

| Fact | Value |
|---|---|
| PR#55 — task backlog + per-tenant fairness (2026-08-07) | Closed 10 outstanding code tasks from an independent review swarm, most substantively: **per-tenant fair consume ordering** (`services/shared/fairness.py`, new) — WS-4/WS-5 round-robin each consume batch by tenant instead of raw FIFO so one flooding tenant can't occupy every consecutive turn ahead of another; reorders only, never drops (the single serial consumer thread per topic ruled out a token-bucket delay); default on, single-tenant behavior byte-for-byte unchanged; honest scope is bounded to one raw consume batch, proven by a dedicated batch-boundary test. **OpenSearch 3-node HA writer failover, LIVE-KILL-TESTED** (`services/ws3-indexer/storage/test_opensearch_ha_failover_live.py`) — real `make ha-up` cluster, real `docker kill` on a node, write survives via round-robin to a survivor, cluster back to `green` after restart; the test's own kill mechanism was itself broken on the first pass (invoked `docker compose kill` against an override file with no image context, so every run silently skipped the kill and reported a false PASS) — caught and fixed before trusting the result, then caught AGAIN in code review (a skip-on-any-failure path masked a genuine kill/restart error as a clean skip) and hardened to hard-fail instead. **WS-5 LLM dedup** — bounded per-event-id cache so a redelivered event isn't re-billed to the LLM; review caught it storing/returning shared dict references (a downstream mutation would corrupt the cached entry) — fixed to copy. **Sanitizer gap closed** — `unmapped.*` (any depth) and `api.request.data` added to the free-text log-injection sanitize path, recursive over nested dict/list values. **`_MemoryBus.consume()` race fixed** — check-and-pop made atomic under a lock released before yielding (avoids a self-deadlock when a handler produces back onto the same bus mid-consume-loop). **Detection-quality precision/recall/F1 canary** (`docs/detection-quality.md`, `tools/detection_quality_eval.py`, new) — engine-vs-hand-labeled-corpus agreement, two deliberately adversarial labels, floor-0.5 regression trip-wire, wired into `run_all_tests.sh`. Also fixed two real regressions found only while verifying the above: `engine.py`/`tenants.py` broke 14 tests by importing `shared.log` without ensuring `services/` was on `sys.path` for callers that import the engine directly (not through `main.py`); `test_bus_memory_race.py` existed but was never wired into the CI gate. **Process note**: this PR was started by one Claude Code session, stopped mid-work, picked up and finished by another in a later session — verified the stopped work was sound (ran the real test suite, found it genuinely green) before building on it, rather than discarding or blindly trusting it. CI went red twice post-merge-readiness on real, non-cosmetic-adjacent bugs (a `mypy` type error, two `ruff` findings from the review-fix commit) — both caught by CI and fixed same-day, not silently ignored. |
| Deep-hardening pass (v0.4→v0.5 bridge, 2026-07-16) | Full P0+P1+P2 audit-and-fix pass across parser logic / detection engine / pipeline robustness, `717f4ed`..`2f807ec`. **P0** (detection-integrity bugs, one commit `9e2745b`): window-poisoning + non-numeric-time fail-closed, deque/Redis window-counter redelivery parity (member dedup), unbounded window-key eviction, parser-registry routing rewrite (source_type-authoritative, no more silent mis-route/drop), vmware port-crash guard + any-parser-exception-dead-letters-one-record, IP-octet bounds (bad IP dropped not dead-lettered), `status_from_outcome()` shared helper (no more hardcoded `"Success"` masking failed logins), content-hash fallback for `alert_key()` (no-ingest-id events no longer collapse onto one alert). **P1** (`c84c8f6`, `625f4f8`, `7e354bb`): `/health` now probes the bus (503 when degraded) + compose healthchecks on ws1-5, `tools/dlq_peek.py` DLQ inspector, OpenSearch index() transient-vs-permanent retry, HTTP server read timeouts, dashboard+inventory bound to `127.0.0.1` (syslog 5514 stays open — must receive remote logs), shared `to_epoch_ms()` (epoch/ISO/FILETIME, replaces 9 copy-pasted heuristics), rule noise tuning (service-account allowlist on after-hours-admin, documented brute-force/impossible-travel tradeoffs). **P2** (`b645b13`..`2f807ec`): `in`/`contains` grammar operators, cross-source severity rubric (`SEV_BY_CATEGORY`) + validated `siem.sector` override across all 10 parsers, `/metrics` endpoint (per-topic acked/failed/deadlettered + ws1 ingest-edge counters), backpressure depth-watchdog on internal topics (`normalized.events`/`scored.events`/`ai.requests`), IPv6 capture + full 0-7 ASA severity map, opt-in live Redis/OpenSearch test lane (`make test-live`), per-event anti-dormancy gate rewrite (`check_rule_producers.py` now proves group_by/distinct_field satisfiability on an event that ALSO matches the rule's own selection, not just satisfiable somewhere across all fixtures). **Live-verified 2026-07-16**: real Docker stack up, `make test-live` PASS against real Redis (XAUTOCLAIM/PEL/DLQ) + real OpenSearch (idempotent upsert, real 409 CAS conflict), devkit-feeder → real brute-force burst → real alert landed in `alerts-*` with AI triage, replay deduped. |
| ARGUS rebrand | Complete (`717f4ed`, 2026-07-15): 239 occurrences across 42 files, GitHub repo renamed (old URL redirects), `ARGUS_API_KEY`→`FENGARDE_API_KEY`. Second rename since (see below) landed the FENGARDE name; both are one continuous history, not two separate products. |
| FENGARDE rebrand | Adopted on PR#2's branch ahead of `main` (`FENGARDE_API_KEY`, `FENGARDE_RBAC_DB`, `fengarde-sec` throughout); made repo-wide by this merge, including the GitHub repo itself (renamed to `github.com/supermhel/fengarde` 2026-07-18, old URL auto-redirects). |
| Latest release | **v0.5.0** (tag `v0.5.0`, 2026-07-23, commit `c3ec328`) — `v0.4.0` (2026-07-12, commit `153da94`) tagged retroactively in the same pass; both pushed and confirmed live on GitHub. Release notes: `docs/releases/v0.4.0.md`, `docs/releases/v0.5.0.md` |
| License | Apache-2.0, public, `github.com/supermhel/fengarde` |
| Parsers shipped | 17: Linux SSH, Cisco ASA, Active Directory, VMware vSphere, generic syslog, Windows Event Log (incl. account-change 4720/4722/4726/4728/4732), DB audit, MCP/AI-agent tool-call audit (v0.4 P1), OPC UA/OT audit (v0.4 P2), n8n automation-platform audit (v0.4 P3), DNS query log / Kubernetes audit / CEF / AWS CloudTrail (v0.5 A4), Sysmon process/network/file (P0-3, 2026-07-21 audit fix plan — the first class-1001 File System Activity producer, closing a documented total gap), Modbus/TCP protocol-anomaly detector (M7 OT expansion, 2026-07-22), inventory-diff OT device detector (M7 Track Y, 2026-08-05 — normalizes the inventory worker's new-device notifications into OCSF Network Activity events) |
| v0.5 Track A4: DNS/k8s/CEF/cloud parsers (done) | `services/ws2-normalization/parsers/{dns_query,k8s_audit,cef,cloudtrail}.py` — closes the long-standing class-4002 (DNS/HTTP Activity) gap flagged since v0.3 (`contracts/detection-coverage.md`), adds the first Kubernetes and cloud-control-plane producers, and a vendor-neutral CEF parser that feeds existing `common_*` rules rather than adding a new one (documented as its actual value, not an oversight). 5 new rules ship with real producers and pass the A6 anti-dormancy gate on the first try: `common_dns_exfil`, `dc_privileged_container`, `cloud_root_console_login`, `bank_mass_card_read` (needed one additive field, `unmapped.db.object`, added to the existing `db_audit.py` parser — zero behavior change to its existing output), `common_rapid_account_lifecycle` (reuses `windows_eventlog`'s existing 4720/4726 producers with one new fixture). All 24 shipped rules pass `tools/validate_rules.py` + `tools/check_rule_producers.py`; `tools/coverage_gate.py`'s hand-synced WS-2 test list was updated for the 4 new parser test files (measured 90%, floor 88%, same "keep the list in sync" lesson as the 2026-07-19 CI-gate incident). |
| Detection rules shipped | 28 (+1 from PR#24's Modbus OT expansion, +1 new OT inventory-diff rule 2026-08-05): brute-force, brute-force-sourceless (P0-2, groups on target host instead of source IP so a sourceless local-auth spray is still caught -- live-proven on real Splunk attack_data, closes 5 of 6 false negatives the audit found), port-scan, lateral-movement, password-spray, privileged-group grant, after-hours admin, bank DB priv-esc, DC mass-VM-delete, impossible-travel (v0.4 P4), agent credential-file access / tool-call burst / prompt-injection indicator / destructive-command / egress-non-allowlisted-domain (v0.4-v0.5 P1), OT write-outside-maintenance / new-engineering-connection / config-change / Modbus unauthorized-write / new OT device on segment (v0.4 P2 + M7 + M7 Track Y), n8n new-webhook-exposed / workflow-modified-after-hours (v0.4 P3), DNS exfil / privileged-container-create / cloud-root-console-login / mass-DB-object-read / rapid-account-lifecycle (v0.5 A4), beaconing (v0.5 A3, periodicity primitive). 27 of 28 carry an optional, shape-validated MITRE ATT&CK/ATT&CK-ICS/ATLAS tag (confirmed 27/27 tagged rules empirically fire, `eval/attack/fire_check.py`, PR#24 + OT inventory-diff rule); `agent_tool_call_burst` deliberately has none (no defensible single-technique mapping). `ot_new_device_on_segment` (M7 Track Y) now has a real producer (WS-6's `bus_consumer.py`, M7 Track Y follow-up, 2026-08-05) — see its row below for what's zero-infra-proven vs. still not live-Docker-verified. |
| B4 rule hot-reload (v0.5, done) | Opt-in mtime-poll watcher (`RULES_RELOAD_INTERVAL_S`, default 0 = off, byte-identical to pre-B4 behavior): `Detector.reload()` re-parses `contracts/rules/`+allowlists and atomically swaps in the new set, fail-closed on a malformed edit (old set kept, logged loudly). Window-state semantics documented in `services/ws4-detection/main.py`: counters key by rule id, so unchanged rules keep in-flight windows across a reload. `services/ws4-detection/test_hot_reload.py` (zero-infra). **Caveat surfaced by this work**: `contracts/` is baked into the ws4-detection image via Dockerfile `COPY`, not volume-mounted, so hot-reload as shipped only fires on a bind-mount/non-Docker deployment or after an image rebuild — a live-Docker demo of hot-reload picking up a host-side edit with no rebuild is a real follow-up gap, tracked here rather than silently assumed to work. |
| C2 dashboard auto-refresh (v0.5, done) | `services/ws7-dashboard/index.html` polls `/api/alerts` every 10s, only when data is LIVE (mock mode has nothing to poll) and the tab is visible (`document.hidden` guard), and skips the DOM rebuild entirely when the fetched alert set is byte-identical to what's on screen (protects an in-progress, unsaved triage-note edit from being clobbered by a background re-render). SSE/WebSocket push was considered and deliberately deferred — polling against the existing nginx proxy adds zero new endpoints/failure modes. Live-verified on Docker Desktop. |
| C3 MITRE coverage heatmap (v0.5, done) | `tools/validate_rules.py` gained an optional `mitre: {framework?, tactic, technique}` shape check (ATT&CK/ATT&CK-ICS/ATLAS id patterns); `services/ws4-detection/main.py::make_alert` now passes the rule's `mitre` block onto every alert (additive field, `contracts/opensearch-mappings/alerts.json` mapping_version bumped to 3 to index it as keyword); the dashboard's new "Coverage" view (`services/ws7-dashboard/index.html`) renders a tactic x technique heatmap shaded by real alert counts, reading `GET /api/rules` (new nginx proxy to ws3-indexer's existing `/rules` route) plus live alerts. Untagged rules are listed separately, not hidden. Live-verified end-to-end on Docker Desktop: `common_bruteforce`'s real fired alert showed "1 alert" under T1110/TA0006. **Two real, previously-undetected bugs found and fixed while verifying this live** (not introduced by this work, just never live-tested before): (1) `services/ws3-indexer/rules_view.py` and `webhooks.py` computed their `contracts/` path via a fixed parent-count (`_HERE.parent.parent`) that's correct on a host checkout but wrong inside the container's flatter layout, so `GET /rules` (M4.3) and outbound webhook config loading had returned empty/nothing on every live Docker deployment since they shipped — fixed with the same dual-path-probe helper `ws4-detection/main.py` already used, and the ws3-indexer Dockerfile was also missing a `COPY contracts` entirely (present on ws4-detection, absent on ws3-indexer) so the directory wasn't even there to find. (2) the dashboard's `getAlerts()` never mapped `rule_id` from the OpenSearch alert document into the JS alert object, so the heatmap (and any future feature joining alerts to rules) silently couldn't match a fired alert back to its rule; fixed by passing the field through. |
| Rule engine | Boolean grammar + comparison operators (`gt/gte/lt/lte/ne`) + allowlist suppression (`not_in`) + time-of-day predicate (`outside_hours`), class_uid prefilter buckets. Two CI gates on `contracts/rules/`: `tools/validate_rules.py` (B4) and `tools/check_rule_producers.py` (A6 anti-dormancy — v0.4: now runs the real enrich() step too, not just parsers). Grammar documented in `contracts/sigma-convention.md`. **v0.4 lesson**: a rule sharing `class_uid` with another source's producer needs an explicit `siem.source_type` selection or it can mis-fire/pool counters across sources — see `contracts/detection-coverage.md`'s "Cross-source rule scoping" note |
| Backpressure (B2) | Ingest-edge shedding: WS-1 syslog listener token-bucket (`SYSLOG_MAX_EVENTS_PER_SEC`, default 2000/s) sheds excess datagrams before the bus; no mid-pipeline MAXLEN trim (would drop unconsumed = audit violation). Depth watchdog (`Bus.depth()`, `RAW_EVENTS_DEPTH_WARN`) is monitoring-only. Opt-in zero-loss fallback: `BoundedSpool` (disk-backed, `SYSLOG_SPOOL_PATH`) replays shed/dropped events; bounded, `events_lost` counts overflow. |
| Enrichment (A5) | WS-2 post-normalize stage (`services/ws2-normalization/enrichment/`) adds OCSF-additive `src_endpoint.reputation` (local IOC list) + `src_endpoint.location` (local CIDR→country map). Offline/air-gap-safe, additive, fail-open. **Consumed as of v0.4**: `common_impossible_travel.yml` (P4) is the first rule reading `src_endpoint.location.country`. |
| Triage workflow | Status + note per alert (WS-3 triage API port 8013 + dashboard UI). Concurrent writes protected at two layers: in-process lock (single replica) + OpenSearch `_seq_no`/`_primary_term` OCC (`find_alert_versioned`/`index_cas`, bounded retry, honest 409 on exhaustion). CAS wire format unit-tested via fake transport (`test_storage_cas.py`); not yet exercised against a live cluster. Container nginx path validated by config review only — live-stack smoke test still pending |
| Incident-report hook (v0.4 Track R) | `POST/GET /alerts/{id}/report` on the WS-3 triage port (`services/ws3-indexer/reporting.py`). Builtin generic-template backend (open, zero AI/network dependency); optional `REPORT_BACKEND=http` seam for a paid backend (fengarde-sec) — frozen contract in `contracts/reporting.md`. Every report is `status: "draft"` with a mandatory disclaimer, enforced structurally (schema-validated, not by convention); citations optional (additive-field discipline). Dashboard has a "Rapport" button per alert row, renders as text (no innerHTML). |
| Opt-in auth (v0.4 Track S) | `FENGARDE_API_KEY` shared-secret on the WS-3 triage + WS-6 inventory APIs (`services/shared/authz.py`); opt-in dashboard basic-auth via `infra/docker-compose.auth.yml` override; opt-in Redis `AUTH` via `REDIS_PASSWORD`. All default OFF (unset = fully open, matching v0.1-v0.3 behavior) — a real deployment opts in. OpenSearch/Redis/Dashboards ports now bound to `127.0.0.1` by default (were `0.0.0.0`); OpenSearch's own security plugin stays disabled (documented scope cut, see `SECURITY.md` §2). |
| Proven live | Full 7-workstream stack on real Docker/Redis/OpenSearch (not just zero-infra) — see build plan §"Docker — RESOLVED". **v0.4 S + R verified live 2026-07-10** (`38341ce`): with `FENGARDE_API_KEY` set, the pipeline produces alerts end-to-end; report generation through the dashboard nginx proxy → ws3 lands in `reports-*` with `status=draft`+disclaimer; ws3/ws6 return 401 without the key / 200 with it; nginx injects the key server-side. The container-to-container triage/report path is no longer config-review-only. That live run also caught + fixed two blockers the zero-infra gate can't see (a flow-style compose YAML break from the `${REDIS_PASSWORD}` interpolation, and ws2 missing PyYAML for the A5 enrichment import). |
| AI triage | Real Ollama integration + StubLLM fallback (`services/ws5-ai/llm_adapter.py::make_llm()`) |
| Hardening series (2026-07-14/15, post-rebrand) | Deep-audit P0/P1/P2 pass, all zero-infra-gated: **P0** (`9e2745b`) window-poisoning/NaN poison-pill fixes, memory-window redelivery dedup + idle-key eviction, parser registry routing rewrite (unreachable parsers fixed, ambiguous payloads dead-lettered), shared `status_from_outcome()`; **P1** (`c84c8f6`,`625f4f8`,`7e354bb`) `/health` 503-on-bus-deaf + compose healthchecks, slowloris timeouts, dashboard/inventory loopback binds, OpenSearch transient-retry, `timeutil.to_epoch_ms` (FILETIME/ISO), rule tuning + `tools/dlq_peek.py`; **P2** (`b645b13`,`7ea6010`) `in`/`contains` operators (fail-closed, no regex), IPv6/ASA-severity parser tail, anti-dormancy gate now per-event (group_by/distinct_field satisfiable on an event matching the rule's own selection). Rebrand ARGUS→FENGARDE (`717f4ed`); dashboard fully English (#1). |
| Open-core split | **Decided** (2026-07-01, via `/plan-ceo-review`): this repo stays fully open forever; FENGARDE-Sec (trained model + regulatory compliance) is the paid, closed layer in a separate repo. **v0.4 made this concrete**: `contracts/reporting.md` is the first real additive-field contract between the two repos (fengarde-sec's Track R implements the paid backend side against it). |
| Bus backend | Redis Streams (real) + in-memory (tests). **Kafka is NOT implemented** despite older docs mentioning it as a "prod backend" — see architecture review §3 R-A. |
| Security posture | Stored-XSS in dashboard: **fixed** (`35f80fc`). Poison-message DLQ, input validation, prompt bounds: **fixed** (`a60e6d4`). Opt-in auth (v0.4 Track S, see row above) — no auth is still the *default*, same trade-off as before, now with a real opt-in path. **CodeQL's first-ever live scan (2026-07-18/19) found 5 HIGH alerts, all fixed at the design level, zero dismissed** (`958ed5a`): tenant_id path-injection in `rules_view.py` closed by never letting request data into a path expression (trusted-dir glob + stem match — two intermediate "sanitize the constructed path" attempts still left taint, correctly re-flagged); the RBAC first-boot admin password redesigned to operator-supplied via `FENGARDE_ADMIN_PASSWORD` (service never generates/logs/stores plaintext; env unset + empty store = fail-closed, loud warning). Open CodeQL alert count on `main`: 0. |
| CI (post-merge, 2026-07-19) | All three workflows green on `main`: CI (7 jobs), CodeQL, Scorecard. Fixed since PR#2 landed red: ruff F401 in `runner.py`; `ossf/scorecard-action@v2` (no such floating tag — pinned `v2.4.3`); quality job missing `hypothesis`; coverage gate's hand-synced test list predating the merge (WS-3 read 50% because the M4 code was in `--source` with its suites unlisted — synced, re-measured WS-2 90% / WS-3 77%, and the floor set is now **88/65/45/40/60** (ws2 / ws3 / ws3-reports / ws4 / ws6 — see `tools/coverage_gate.py::TARGETS`); note WS-3's floor has since moved down from 75 to 65 as its hardened surface grew). **Three new gates added** (`f8a10e1`/`410515a`): `pip-audit` (CVE audit of every pinned requirements.txt — the deliberate replacement for disabled Dependabot), `docker-build` (all 8 images must build; previously nothing in CI proved it), `actionlint` (lints the workflows themselves; immediately caught a real dead-var in the mypy step). **Supply-chain pinning** (`5a8e450`): every workflow `uses:` SHA-pinned, all 8 Docker base images digest-pinned, least-privilege `permissions:` on all workflows — Scorecard alerts 54→19. The remaining 19 are accepted, not open work: 11 want pip *hash*-pinning (full transitive lockfiles — declined as maintenance-disproportionate given version-pins + the pip-audit CVE gate), 8 are practice/settings-level (BranchProtection off by choice, CodeReview/Maintained/CITests accrue with history, CII badge is an external signup, DependencyUpdateTool = the deliberately-disabled Dependabot). |
| Forward roadmap | The combined M1-M7 execution plan (owner's internal roadmap doc — not kept in this public repo's working tree; see the note in §4). Status as of PR#24 (2026-07-23), superseding the `8b3f450` snapshot this row was first written against: **M1 correctness gates — done, chaos gate CLOSED**: `make chaos` run live 2026-07-18 and PASSING (`scenarios=40 lost=0 duplicated=0`, 5 services SIGKILLed mid-replay) after 4 harness bugs were found and fixed across 4 runs. See §1's chaos-gate row and §5. **M2 proof artifacts — CLOSED 2026-07-22** (bench numbers real and in README; supply-chain root-cause fix + Dependabot/SBOM/CodeQL/Scorecard wired; quality floor: ruff blocking, **mypy now blocking too** — re-measured the stale "20 findings" baseline before trusting it (a live re-run found 47 across all 8 workstreams, not 20), fixed all 47 with narrow behavior-preserving changes, verified 0 errors per workstream + full `run_all_tests.sh` pass, removed CI's `|| true`; mutmut CLOSED 2026-07-22, real baseline measured, not started previously: added as a CI-only dependency (not a service requirements.txt entry), configured via `pyproject.toml`'s new `[tool.mutmut]` (native Windows mutmut isn't supported -- upstream issue #397 -- so this session's actual run happened under WSL; CI runs it natively on ubuntu-latest). Deliberately scoped to `services/shared/sessions.py` only for this FIRST pass, not the whole `services/shared/` directory -- each mutant re-runs the test file, and a directory-wide run against all 12 zero-infra shared test scripts would take hours in this environment; the narrower, real number now beats a directory-wide number that would never finish. New `tools/test_mutmut_shared.py` (a pytest shim exercising `SessionStore`'s memory backend in-process -- two earlier versions of this shim failed silently: one subprocess-invoked the standalone test script and mutmut saw zero in-process coverage, the other imported via a `sys.path` hack that produced a module key mutmut couldn't match to any mutant). **Measured result**: 142 mutants generated, 92 in `RedisSessionStore` (untested here -- needs live Redis, correctly out of this pass's scope, not silently miscounted as covered), 50 in the covered surface (`SessionStore` + `make_session_store`), 36 killed / 14 survived (72% kill rate on covered code). All 14 survivors are in `make_session_store`'s error-message/branch details, not `SessionStore` itself -- a real, disclosed follow-up, not silently passed over. `make mutation-test` (new target) runs it; CI's `quality` job runs it informational-only (same "measure first, gate later" sequencing mypy went through before flipping blocking). Expanding scope to the rest of `services/shared`, or wiring `RedisSessionStore` in via `make test-live`, stays a real, explicitly-tracked follow-up. **M3 product completeness — done** (the dashboard session-login UI + CSRF pass, previously the two real open items under this milestone, closed 2026-07-17 — see the new row below). **M4 MSP-grade (the launch gate) — done**: multi-tenancy, RBAC, versioned REST API + OpenAPI spec, HMAC-signed webhooks, entry-points plugin interface, ops lifecycle (schema migration, backup/restore, disk guardrails) — see the new summary rows below. **M5 NIS2 public template layer — done**: deterministic German/English generator, eval harness (72 drafts, CI-gated), dashboard button (browser-verified), `make nis2-demo`. **M6 LAUNCH — EXECUTED 2026-07-30**: owner manually posted across 4 communities (matches the previously-planned `docs/posts/launch-checklist.md` sequencing, r/netsec → r/selfhosted → Show HN → r/blueteamsec — exact per-community breakdown not itemized here, just the total) and answered comments/questions there. See §5 below for the full outcome record, which needs periodic follow-up as traction changes. Two locked decisions held through launch: NIS2 template layer is public in this repo (fengarde-sec keeps model/legal layer via the `contracts/reporting.md` seam); no posting before MSP readiness (M4) — both honored, launch happened only after M4 went green. v0.4 Track X carry-overs: **B3 dual-backend test — CLOSED (verified 2026-07-21, zero new code needed)**: `services/shared/test_runner.py` already parametrizes all six behavioral bodies (ack-once, fail-no-ack, redelivery→DLQ, flaky-eventually-acked, multi-topic, threaded serve) over memory+redis via `_BACKENDS`, and CI's `redis-integration` job runs exactly that suite with `BUS_BACKEND=redis` against a redis:7 service container — this is what v0.3 §B3 asked for; it had been built by later work without the backlog entry being retired. **B4/C2/C3 — CLOSED 2026-07-21** (rule hot-reload, dashboard auto-refresh, MITRE coverage heatmap; see the dedicated summary rows above). **Remaining A4 parsers — CLOSED 2026-07-21**: DNS/k8s/CEF/cloud all shipped (see the v0.5 Track A4 row above); **S7/PROFINET re-investigated, still deferred but for a narrower, evidenced reason** (`docs/superpowers/specs/2026-07-21-s7-profinet-decision-gate.md`): S7-1500 actually ships a real, publicly-documented RFC 5424 syslog security-event feed (the original "proprietary-shaped" reasoning was too broad), but the concrete MSGID/event vocabulary needed to parse it honestly sits behind a Siemens support login this pass didn't have access to (a direct fetch 403'd) — deferred on an access gap, not a documentation gap, with a concrete unblock path recorded. **Periodicity primitive — CLOSED 2026-07-21**: `services/ws4-detection/window.py` gained `hit_periodic()` on both backends (reuses the exact window state `hit()` already maintains, no new storage — coefficient-of-variation of in-window inter-arrival deltas, `None` when fewer than 3 events, never fabricated as 0), `engine.py`'s `Rule.periodicity` wires it into evaluation, `tools/validate_rules.py` shape-checks the new `siem.periodicity: {max_cv}` block, and `common_beaconing.yml` (class 4001, cisco_asa's existing accept producer) ships with a real firing test (`test_v05_beaconing.py`: fires on a regular cadence, does not fire on an irregular cadence of the same count) plus backend-parity tests (`test_window_periodic.py`). Full design + stated limitations (trivially evaded by jitter, no multi-field group_by) in `docs/superpowers/specs/2026-07-21-periodicity-primitive.md`. **B5 HA design doc — CLOSED 2026-07-21** (moved to fengarde-sec's `docs/from-public-repo/specs/` in the 2026-07-31 doc audit, §4): decision only, no code, per the v0.3 plan's own "decide, don't build" scope for this item. Recommends Redis Sentinel (not Cluster — nothing here needs sharding, and Cluster's multi-key restriction would constrain future work for no throughput benefit this workload needs) and a standard OpenSearch multi-node cluster with replica shards (index templates already set `number_of_replicas: 1`, zero app-code change). Documents that most of the application-layer multi-replica story is ALREADY solved by prior work — `RedisWindowCounter` (T6), real Redis Streams consumer groups, deterministic `alert_id`, OpenSearch CAS, and this session's `RedisSessionStore` — without any of it having been built as an "HA project." WS-6 inventory (SQLite) remains flagged as genuinely out of scope, not silently assumed solved. All Track X items are now closed except the ones this doc itself scopes as deliberately deferred (WS-6 migration) or already closed elsewhere (S7/PROFINET, §1's dedicated row). |
| MSP-grade (M4, done 2026-07-16) | Multi-tenancy (tenant-scoped indices, per-tenant rule enablement, `tools/test_multi_tenant_isolation.py`); RBAC (`FENGARDE_RBAC_DB` opt-in, SQLite users + scrypt hashing + sessions + roles, `services/shared/{users,sessions,rbac}.py`); versioned REST API (`contracts/triage-api.yaml`, `/api/v1/...` alongside the unchanged bare paths); outbound HMAC-signed webhooks (`contracts/webhooks/`, `services/ws3-indexer/webhooks.py`); entry-points parser/rule plugin interface (`docs/plugin-development.md`); ops lifecycle (`services/shared/users.py` schema migration via `PRAGMA user_version`, `tools/backup.py`/`restore.py`, `tools/migrate_opensearch.py`, `services/shared/diskguard.py`). **A real gap found and disclosed while building this, not hidden**: OpenSearch ILM/retention policies were never actually installable on a live cluster (schema mismatch, see §2 below) — everything else in M4 is real and tested, this one pre-existing issue surfaced during the "versioned index mappings" work. |
| WS-6 inventory tenant isolation (F1 close, 2026-07-30) | The 2026-07-29 adversarial architecture review (`review_architecture.md`) correctly found M4's multi-tenancy pass never reached WS-6 inventory: no `tenant_id` column, no route filtering, one shared API key with no tenant awareness — a same-MAC collision from two tenants silently overwrote a record, and any caller could enumerate every tenant's inventory. Fixed same day, `4d7467c`: `tenant_id` added to the schema (`PRIMARY KEY (tenant_id, mac)`), every route (`GET /assets`, `/assets/resolve`, `/assets/{mac}`, `POST /assets/upsert`) scoped to it, a pre-fix on-disk DB migrates in place, `"default"` everywhere for any caller that never mentions tenant_id (zero migration for the only deployment topology that exists today). Proven by `services/ws6-inventory/test_tenant_isolation.py` (7 scenarios: same-MAC collision, list/resolve scoping, migration, malformed-id rejection, real HTTP). **A follow-up review (`independent_review_of_fixes.md`) then caught that this fix was still incomplete**: `tenant_id` was a caller-supplied, format-validated-but-unauthenticated query param — any caller holding the one shared `FENGARDE_API_KEY` could still enumerate any tenant by guessing its id; the data model changed, the trust boundary didn't. Closed the same day: `FENGARDE_API_KEYS` (opt-in, `tenant:key,tenant:key,...` plus a `*:key` admin entry), `services/ws6-inventory/authz.py::check_tenant_scoped_auth` (**superseded 2026-07-31 by the keystore, see the next two rows — `check_tenant_scoped_auth` no longer exists; `authz.py` today only holds `warn_if_disabled()`, and `app.py::_check_auth` is one `KEYSTORE.verify()` call**). A tenant-scoped key has its own `tenant_id` forced onto every read AND write, silently overriding any `?tenant_id=`/body value it asks for instead — same "scope narrowing, never a confirming rejection" convention WS-3's `triage_api.py::_list_tenant_filter` already uses. `FENGARDE_API_KEY` (singular) is untouched and still governs when `FENGARDE_API_KEYS` is unset — existing single-tenant deployments see zero behavior change. Proven by 4 new scenarios in `services/ws6-inventory/test_auth.py` (cross-tenant read blocked even when explicitly requested, cross-tenant write via a spoofed body `tenant_id` blocked, unknown key 401s, admin key unrestricted). **Deliberately not built**: WS-6 does not get WS-3's full cookie-session RBAC (login flow, roles, CSRF) — it's a headless service with no browser client, and a rushed session layer here would be exactly the "half-finished security boundary" the independent review warned against; a static per-tenant secret was judged the right-sized control for this surface. `bash run_all_tests.sh` green with this change included. |
| WS-6 inventory: per-tenant keys hashed at rest (F1 second follow-up, 2026-07-30) | The row above shipped `FENGARDE_API_KEYS`, plaintext `tenant:key` pairs compared live via `hmac.compare_digest` — real isolation, but the key material sat in cleartext for the life of the process/env, and provisioning meant hand-editing an env var. Replaced with a real keystore: `services/ws6-inventory/keystore.py::TenantKeyStore`, one SQLite table (`api_keys`, same file as `INVENTORY_DB`), scrypt hash per tenant (`hashlib.scrypt`, identical KDF/cost params/`scrypt$salt$hash` format to `services/shared/users.py::hash_password` — the one other place this repo hashes a secret) — no raw key ever touches disk. A fast unsalted SHA-256 `lookup_hash` column narrows each request to one candidate row before the (deliberately slow, ~50ms) scrypt verify runs, so auth cost stays O(1) per request regardless of tenant count instead of O(n) scrypt calls — the latter being both a real latency problem and a cheap computational-DoS lever at MSP scale. **Backward compatible by construction, not by a written migration step**: `ensure_legacy_keys_migrated()` (first-boot bootstrap, same shape as `shared/users.py::ensure_first_boot_admin`) runs once at `app.py` startup — if the keystore is empty and `FENGARDE_API_KEYS` or the original `FENGARDE_API_KEY` is set, that EXISTING key value is hashed and provisioned as-is (FENGARDE_API_KEYS wins if both are set), so an operator's current credential(s) keep working completely unchanged after upgrading; a keystore that already has a row is never touched again by migration, so a stale leftover env var can't silently overwrite a real provisioned key. New `services/ws6-inventory/manage_keys.py` CLI (`provision`/`list`) for ongoing key issuance/rotation — a raw key is shown exactly once, at creation, never logged or stored. `FENGARDE_API_KEYS`/`FENGARDE_API_KEY` are now consulted ONLY as migration input, once; the keystore, once seeded, is the sole runtime auth source (`app.py::_check_auth` collapsed from a 3-way legacy/tenant-keys/keystore branch to one keystore check). **Deliberately still not built**: WS-6 does not get WS-3's full cookie-session RBAC (login flow, CSRF) — same rationale as the row above, unchanged. Proven by `services/ws6-inventory/test_keystore.py` (11 store-level scenarios: never-plaintext-at-rest, cross-tenant independence, rotation invalidates the old key immediately, admin marker, both legacy-migration paths + their precedence + no-op + never-overwrite-a-real-key) and `services/ws6-inventory/test_auth.py` (7 HTTP-level scenarios, same guarantees end-to-end through the real `Handler`, plus rotation and migration proven live over a real server). `ruff check services/ws6-inventory/` and `mypy services/ws6-inventory --explicit-package-bases --ignore-missing-imports` both clean; full `bash run_all_tests.sh` green with this change included (`test_keystore.py` added to the gate). |
| WS-6 inventory: keystore hardened per independent review (F1 third follow-up, 2026-07-31) | An independent adversarial review (`cavecrew-reviewer`, project convention after Codex was unavailable) of the second-follow-up keystore found four real defects, all now fixed, plus scope/rotation gaps addressed proactively. **(1) scrypt was the wrong primitive** — it exists to slow an offline attacker brute-forcing a low-entropy *password*; these keys are `secrets.token_urlsafe(32)` 256-bit random tokens, so a memory-hard KDF bought nothing but a ~150ms/request throughput ceiling and an unauthenticated-DoS lever. Replaced with **HMAC-SHA256 keyed by a server-side pepper** (`FENGARDE_API_KEY_PEPPER`), the standard GitHub/Stripe/AWS model for high-entropy tokens; verify is now sub-millisecond. The separate fast `lookup_hash` column the scrypt round needed is gone — one fast `key_hash UNIQUE` column does both jobs. **(2) A duplicate key across two tenants** in `FENGARDE_API_KEYS` used to raise an unhandled `sqlite3.IntegrityError` at import → service wouldn't boot (a real backward-compat regression vs the plaintext era); now `provision()` raises a catchable `DuplicateKeyError` and migration *skips* the colliding entry with a warning, first entry still migrates. **(3) The prior round's decoy-timing "defense" was inverted** (miss ran two hashes vs one on hit, 301ms vs 156ms — it *created* the asymmetry it claimed to remove); dropped entirely, moot now that verify is a single fast keyed hash. **(4) `provision()` never validated `tenant_id`** — a key for `"ACME Corp"` authenticated then 400'd on every request (silently useless credential); now validated at provision AND skipped-with-warning at migration, reusing `store.py::_validated_tenant`. Also added, same schema change: **read_only/read_write scopes** (`do_POST` 403s a read-only key before it can write — a leaked read key can't poison inventory that feeds detection); **multiple independently-revocable keys per tenant** (`key_id` PK, `revoke()`, so rotation is zero-downtime: provision new → cut over → revoke old, no forced gap — the prior round overwrote instantly); **`last_used_at`** (throttled to ≤1 write/hour/key); a **drift warning** when a legacy env var is still set but the keystore was already seeded (editing it silently does nothing); and a **weak-key warning** when a short, possibly human-chosen legacy key is migrated (a fast hash gives it less margin than scrypt did if DB+pepper both leak — nudge to rotate). `manage_keys.py` gained `revoke`, `--scope`, and richer `list` (key_id/scope/source/created/last_used). **A second adversarial review of the reworked keystore then found two more real risks, both fixed**: (a) a persistent DB written by the scrypt round has an incompatible `api_keys` schema (no `scope`/`key_id` column), so this round's `verify()` raised `OperationalError` → app.py 500 on every request → silent lockout on container upgrade; now `_migrate_pre_hmac_schema()` renames the incompatible table aside (never dropped — scrypt hashes can't be re-verified under HMAC, but the rows are preserved for forensics) and the empty fresh table lets `ensure_legacy_keys_migrated()` repopulate from the still-set env var, so the common upgrade path is zero-touch. (b) Changing `FENGARDE_API_KEY_PEPPER` after keys exist silently fails every key (fail-closed lockout, no obvious cause); a pepper canary written at first provision now lets `_check_pepper_canary()` detect drift and log a loud, named error at startup. Proven by `test_keystore.py` (26 store-level scenarios) + `test_auth.py` (9 HTTP scenarios incl. read-only-blocks-write, URL-decoded MAC lookup, zero-downtime rotation). `ruff`/`mypy` clean; full `run_all_tests.sh` green. **Still deliberately not built**: WS-6 has no cookie-session RBAC (unchanged rationale — headless service); pepper rotation still invalidates all keys by design (now *detected and announced* rather than silent, but re-provisioning is still the recovery path — documented, out of scope to make seamless). |
| NIS2 public template layer (M5, done 2026-07-16) | `contracts/nis2-de-schema.json` + `services/ws3-indexer/nis2_template.py` — deterministic German/English NIS2 Art. 23 / §32 BSIG draft generator, additive on the existing `/alerts/{id}/report` route (`?template=nis2`). States its own NIS2-vs-DORA scope caveat inline (financial entities are typically DORA-governed, not NIS2). Zero LLM, zero paid dependency — the paid, legally-validated layer stays `fengarde-sec`'s via the existing `REPORT_BACKEND=http` seam, untouched. `docs/nis2-report-generator.md` has the full honest scope/limits. |
| Adversarial repo-wide bug hunt (2026-07-16/17, `6e3fbe4`..`f7f6190`) | A dedicated review pass over the whole M4/M5 surface (not just a PR diff), owner-requested. **Method, honestly stated**: five parallel review subagents across two rounds all terminated early on a session/usage quota before returning verdicts — a real environment limitation, not a false-negative sweep — so every finding below was independently confirmed by direct code reading (parser source, callers, schema/validation, existing tests), not by trusting agent output. Found and fixed 6 real bugs, each with a regression test verified via a revert/run/restore cycle on the fix's own diff: **F1** (HIGH) cross-tenant pooling in WS-4's stateful window counter key; **F6** (MED/HIGH) `active_directory.py` skipping the M1 OCSF field-type guards every sibling parser already has; **F2** (MEDIUM) `GET /alerts/{id}/report` tenant-gate bypass when the alert doc is absent; **F5** (LOW/MED) `LoginRateLimiter` unbounded growth + missing lock; **F4** (MEDIUM) `tools/restore.py` symlink-escape via extract-before-verify; **F3** (MEDIUM) unvalidated `tenant_id` embedded in OpenSearch index names / config paths (reject-at-edge, per owner decision — never normalize, to avoid a *new* cross-tenant merge bug). **A dedicated follow-up review subagent on just the fix commit (`6e3fbe4`) then caught a real incompleteness in F1 itself** (`f7f6190`): the window *counter* key was tenant-namespaced, but `Rule.alert_key()` — the function that computes the actual `alert_id` persisted to storage and returned to API callers — still wasn't, so two tenants firing in the same window bucket on a shared group_by value got the IDENTICAL alert_id and `find_alert()`'s cross-index-by-id lookup could return the wrong tenant's doc; fixed and independently reverify-verified the same way. That follow-up review re-checked F2-F6 too and found no further issues. Full details, discarded false positives, and the "not yet reviewed" coverage-gap list (bus.py/runner.py redelivery, most parser test-coverage depth, diskguard.py, migrate_opensearch.py, chaos_test.py, generate_sbom.py --check, spool.py atomicity, users.py migrate downgrade) are in the review artifact this pass produced — those gaps are a real follow-up item, not silently dropped. `bash run_all_tests.sh` and `ruff check services/ tools/ eval/` are clean with all fixes applied together. |
| M3 remainder: dashboard session login + CSRF (done 2026-07-17) | `services/ws7-dashboard/index.html` gains a login form gating the app behind the M4.2 RBAC session API (`/auth/login`/`/auth/logout`/`/auth/me`, previously real and HTTP-tested but never called from the browser); new nginx proxy path `/api/auth/` (`services/ws7-dashboard/templates/default.conf.template`). **RBAC off (every existing deployment) is byte-for-byte unaffected** — `GET /auth/me` 404s, the login gate is skipped, same convention as every other opt-in auth layer. **CSRF protection** (`services/ws3-indexer/triage_api.py::_check_csrf`, `services/shared/sessions.py`'s new `csrf_token`) is a second, independent layer on top of the session cookie's existing `SameSite=Strict`: every session-authenticated POST must echo a per-login token back as `X-CSRF-Token` or get 403, a no-op when RBAC is off. **Two real bugs found and fixed via actual Playwright/Chromium browser testing, not just the static contract test**: a CSS-specificity trap (`#loginScreen`'s own ID-selector rule silently outranked the `hidden` attribute) and an inline-style `""` "unset" that fell back to a stylesheet `display:none` instead of becoming visible — the second one would have left the app permanently hidden for every RBAC-off deployment had it shipped. Both fixed by always setting an explicit `style.display` value. Verified end-to-end in a real browser: login → CSRF-protected write → reload persists; wrong token 403s; logout invalidates the session and re-locks the app; RBAC-off path confirmed to show the app immediately with no gate. |
| M7 continuous track: MITRE empirical firing check (2026-07-22) | `eval/attack/fire_check.py` (new) closes the gap between `coverage_layer.py`'s DECLARED coverage scorecard and `eval/detection_accuracy/`'s dataset-gated real-world replay: every MITRE-tagged rule is replayed against its own real anti-dormancy producer fixture through the actual WS-4 `Rule.evaluate()` (rule-condition level only — `Detector` is used as a rule loader, so `Detector.process()`'s `class_uid` prefilter and per-tenant disable are NOT exercised; see the §2 row for what that excludes). **Result: 26/26 tagged rules fire** (was 25/25 at first run, +1 from PR#24's Modbus rule). Found and fixed a real harness bug in the process (not a rule bug): stateful-rule synthetic repetitions stepped FORWARD from an already-"now" timestamp and tripped `engine.py`'s 5-minute anti-poisoning clock-skew guard past the first repetition, showing 2 real rules (`common_beaconing`, `common_rapid_account_lifecycle`) as falsely SILENT; fixed by anchoring the last repetition at wall-clock "now" and stepping earlier ones backward into the past. Wired into `make attack-scorecard`, CI's `attack-scorecard` job (blocking), and `run_all_tests.sh`. Full design + the three-claims distinction (declared vs. fired-on-fixture vs. fired-on-real-traffic) in `docs/superpowers/specs/2026-07-22-mitre-fire-check.md`. |
| M7 follow-up: fire_check boundary (negative) probes (2026-07-28) | Firing AT threshold only proves a rule is not dead; a rule that is too LOOSE (off-by-one count, window wider than declared) fires at threshold too, passes the anti-dormancy gate too, and shows up months later as unexplained false-positive volume rather than as a dormant rule. Every stateful rule that fires is now also replayed at `threshold - 1` in-window, and at a full `threshold` spread to span just past `window_seconds`, and must stay silent for both. **Result (2026-07-28, superseded 2026-08-11 -- see the §2 "Rule engine actually enforces every predicate a STATELESS rule declares" row for the current status): 12 of 12 stateful rules hold their boundary; the stateless ones (15, not 14 -- this row's own count went stale) were reported NOT boundary-tested at the time, not counted as passing** — the near-miss of a single-event field match is the entire remaining value space, so no near-miss fixture is generatable and each needs a hand-authored one. Probes are isolated by a synthetic `siem.tenant` (the engine's window counter is keyed `{rule}:{tenant}:{group}` and lives on the Rule instance, so without it the sub-threshold probe inherits the positive probe's counts and reports a defect that is pure harness state). `eval/attack/test_fire_check.py` (new) mutates real rules until they ARE too loose and asserts the gate exits 1 **end to end through `main()`**, not merely that the internal replay returned True — a negative assertion that cannot fail is not a test. It caught the first window probe being too blunt to cite (spacing events `window+1s` apart only fires when the real window is ~`threshold`× too wide, so it could not see a small window error on a high-threshold rule). An adversarial second review then caught four more, all fixed: the gate's FAILING verdict was selected by exact-matching the prose string `"FIRED"` while the passing verdict used a prefix match, so editing the failure message disarmed the gate with every test still green (verdicts are now a machine `status` field); a rule with one probe held and one skipped was counted as having held its boundary; `_hours_confound` guarded both negative probes but not the positive replay, so the first stateful `outside_hours` rule would have been reported dead-on-arrival (off-hours anchoring is now span-aware, and an unconstructable replay fails loudly as a HARNESS defect rather than as a rule defect); and the isolation test passed via `ingest_id` redelivery dedup rather than the tenant namespacing its own docstring described. |
| M7 continuous track: observability -- Prometheus + Grafana (2026-07-22) | New `/metrics/prom` route on `services/shared/runner.py`'s existing health server (`render_prometheus()`) -- hand-rolled plain-text exposition format against stdlib only, deliberately NOT the `prometheus_client` dependency the original plan sketch named, to avoid adding a new runtime dependency to every service's requirements.txt for a handful of counters this module already tracks in-process. Same per-topic acked/failed/deadlettered counts as the existing JSON `/metrics` route (unchanged, additive). Only WS-1..WS-5 use `shared/runner.py`'s `serve()` loop -- WS-6/WS-7 have their own HTTP surfaces and are NOT scraped, disclosed in `infra/prometheus.yml`'s own comment, not silently omitted. `infra/docker-compose.yml` gained `prometheus`+`grafana` services under a new opt-in `observability` compose profile (default `docker compose up` byte-for-byte unaffected, same convention as `docker-compose.auth.yml`); Grafana auto-provisions the Prometheus datasource and one starter dashboard (`infra/grafana/dashboards/fengarde-overview.json` -- throughput/DLQ-depth/failed-rate panels, all backed by real counters; a "backpressure gauges" panel was DROPPED before shipping because no service currently wires the optional `extra_metrics_fn` hook, and a panel with no real data source would be exactly the overclaiming this project's honesty convention exists to prevent). **Live-verified on Docker Desktop 2026-07-22**: `docker compose --profile observability up`, all 5 scrape targets `up` in Prometheus (`/api/v1/targets`), real pipeline counters queryable (`ws4-detection` alone showed 39,802 acked `normalized.events` from the devkit-feeder's live burst), Grafana dashboard auto-loaded (`/api/search` confirms `fengarde-overview`) with the datasource auto-provisioned (`/api/datasources` confirms `Prometheus` -> `http://prometheus:9090`). **OTel tracing is explicitly OUT of scope for this pass** -- it needs collector wiring across all 7 services, a materially bigger lift than the other M7 items; still an ADR-only aspiration (`docs/adr/004-microservice-split-bus-only-coupling.md`), not silently rolled into "observability done." |
| M7 continuous track: OT expansion -- Modbus/TCP protocol-anomaly detector (2026-07-22) | New `services/ws2-normalization/parsers/modbus_anomaly.py`, the second OT source after `opcua_audit.py`, deliberately scoped DIFFERENTLY: Modbus/TCP has no vendor audit-log format at all (bare request/response control protocol, no login-gated vocabulary the way S7's is), so this parser classifies one *observed* frame's function code against the protocol's own public function-code table (Modbus Application Protocol V1.1b3) for 3 anomaly classes (`exception_response`, `unknown_function_code`, `unauthorized_write` -- the last a small, explicitly-declared "expected safe" address-range heuristic, same shape as `opcua_audit.py`'s `_CONFIG_NODE_MARKERS`) rather than "parsing a vendor log" that doesn't exist to parse -- full scope-boundary reasoning in `docs/superpowers/specs/2026-07-22-modbus-anomaly-scope.md`, written specifically so a future reader doesn't mistake it for a second vendor-log parser. New rule `ot_modbus_unauthorized_write.yml` (ATT&CK-ICS T0855 Unauthorized Command Message / TA0106 Impair Process Control) ships with a real producer fixture, passes `check_rule_producers.py`'s anti-dormancy gate and `eval/attack/fire_check.py`'s empirical firing check on the first try. 10 new tests (`test_modbus_anomaly.py`), `tools/coverage_gate.py`'s WS-2 test list updated. Parsers 15->16, rules 26->27. |
| M7 follow-up: rule-health / dead-rule watchdog (2026-08-05) | Closes the gap design-review finding E named (2026-07-29 audit) and fengarde-sec's forward-plan-v2 §3 item 7 called out as still open: "rules that CAN fire" was proven (`eval/attack/fire_check.py`, 26/26) but "rules that ARE firing in production" had zero live signal — a parser regression could silently zero out a rule's firing rate with no distinguishable signal from "no attacks happened." `Detector.record_fire(rule_id)` (`services/ws4-detection/main.py`) stamps a real UNIX timestamp per rule id on every match, keyed by id (not object identity) so it survives `reload()` the same way window state does; `Detector.rule_health_metrics()` returns one gauge per rule THAT HAS FIRED — a never-fired rule is absent, never fabricated as 0/None, matching this project's periodicity-primitive convention. Wired as WS-4's `metrics_provider` into the existing `shared.runner.serve()` hook (the same optional `extra_metrics_fn` seam the 2026-07-22 observability pass left unwired — see the M7 observability row above, "no service currently wires it"; this is the first). Renders on the already-shipped `/metrics/prom` route with zero new infra. New Grafana panel `infra/grafana/dashboards/fengarde-overview.json` id 4 (`time() - fengarde_extra{field=~"rule_last_fired_timestamp:.*"}`) — the panel this project's own honesty convention blocked in the 2026-07-22 pass for having no real data source now has one. 5 new tests `services/ws4-detection/test_rule_health.py` (never-fired absence, real timestamp on fire, second-fire advances it, survives reload, real Prometheus gauge line rendered), wired into `run_all_tests.sh`. `ruff`/`mypy` clean; full `bash run_all_tests.sh` green with this change included. **Not yet live-verified on a real Docker/Prometheus stack** — zero-infra only, same standing caveat as most of this table; the 2026-07-22 pass's live Docker verification of `/metrics/prom` covered the pre-existing topic counters, not this new gauge. |
| M7 follow-up: Sigma-glob operator + partial import layer (2026-08-05) | Cheap partial step design-review finding D (2026-07-29) named toward Sigma-rule portability — the rule grammar had zero wildcard support (`contracts/sigma-convention.md`'s only string ops were exact-equality and bounded `contains`), so none of SigmaHQ's ~3,000 public rules were mechanically portable. New `glob` operator (`services/ws4-detection/engine.py::_glob_match`, Python `fnmatch.fnmatchcase`) supports Sigma-style `*`/`?`/`[seq]`/`[!seq]` wildcards. **Explicitly NOT a regex layer** — ADR-005's no-regex/ReDoS-safety constraint is unchanged; `fnmatch` translates these four metacharacters into bounded, non-overlapping-repetition patterns, which is why wildcards specifically don't reopen the door regex was excluded for. Same fail-closed/bounded-length discipline as `contains` (both operands must be strings, pattern capped at 200 chars, `_KNOWN_OPS` + shape validation added to `tools/validate_rules.py`). 21 new test cases in `services/ws4-detection/test_v03_rule_grammar.py` (wildcard/`?`/character-class correctness, anchoring, missing-field/non-string/empty/oversized-pattern fail-closed paths). A basic partial pySigma translation layer was also added in the same pass (`tools/import_sigma_rules.py`): it covers selection sanitization, dict/list/OR selection shapes, condition rewriting with `and`/`or`/`not`, `contains`/`startswith`/`endswith` modifiers, bounded regex-to-glob translation, and a subset of `siem` fields. **Honest scope**: this is roughly the basic detection/condition layer, maybe 10-20% of real-world SigmaHQ constructs. Still open: full regex fields, additional modifiers (`base64`, etc.), timeframes, references, and more complex condition syntax — this does not make arbitrary SigmaHQ rules importable yet. `ruff`/`mypy` clean; full `bash run_all_tests.sh` green with this change included. |
| M7 Track Y: OT inventory-diff rule (2026-08-05) | Closes the missing-signal gap this task explicitly targeted: the existing OT rules key on OPC UA session/auth events (`ot_new_engineering_connection`) or write operations (`ot_write_outside_maintenance`, `ot_config_change`), but not on inventory state. New `services/ws2-normalization/parsers/inventory_diff.py` normalizes the inventory worker's new-device notifications into OCSF Network Activity events (`class_uid` 4001, `activity_id` 1) with `siem.source_type=inventory_diff`, `siem.sector=datacenter`, `unmapped.ot.sector=ot`, and OT device metadata; new `contracts/rules/ot_new_device_on_segment.yml` watches for unknown OT MACs on the OT segment (ATT&CK-ICS T0864 / TA0108). Rule is single-shot / stateless by design; OT-sector events get `severity_id=4` (High). Parser/routing registered in `__init__.py`, fixture added to `tools/check_rule_producers.py`; new test `services/ws4-detection/test_ot_inventory_diff_rule.py` plus `run_all_tests.sh` entry. Verified green through the full suite and `eval/attack/fire_check.py` (27/27 tagged rules fire, including this one). **NOT YET LIVE -- this rule has no production producer.** Two independent reviews (2026-08-05) both returned DO NOT SHIP on the first cut of this work, and they were right: nothing publishes an inventory-diff notification to `raw.events`, so the only thing that has ever fired this rule is its own anti-dormancy fixture. `eval/attack/fire_check.py` green therefore proves the rule CAN fire, not that it ever WILL -- exactly the declared-vs-empirical distinction §2 of this doc exists to keep separate. What DID land as real, durable behavior: WS-6 now detects a first-ever sighting of a MAC per tenant (`InventoryStore.upsert_with_diff()`), gated behind a per-tenant baseline window (`INVENTORY_BASELINE_SECONDS`, default 3600) so standing the service up against an existing segment populates inventory instead of emitting one alert per device already present, and durable in SQLite so a restart is not mistaken for the whole segment reappearing; the signal is surfaced as `new_device` on `POST /assets/upsert`'s response. 12 scenarios in `services/ws6-inventory/test_new_device_diff.py` (cold-start silence, same-MAC-in-two-tenants independence, late-onboarded tenant baselines on its own clock, restart durability, already-populated tenant treated as baselined not re-baselined, legacy `upsert()` unchanged). **The missing link is transport**: WS-6 is deliberately stdlib-only (its `requirements.txt` documents the `redis` bus dependency as intentionally deferred, and nothing consumes `assets.updates` yet), so wiring WS-6's new-device signal onto the bus is a real, still-open architectural step, not an oversight. Also fixed in this pass: the parser wrote an unvalidated `mac` straight into `src_endpoint.mac` (a malformed or non-string value passed the parser and then dead-lettered on Contract A validation downstream -- now rejected at the edge via `shared.ocsf.valid_mac`), and this branch's own doc-sync had corrupted 10 Markdown table rows in `SSOT.md`/`contracts/detection-coverage.md` with a stray leading `||`, 3 of them pre-existing rows it merely touched. **Follow-up (M7 Track Y follow-up, 2026-08-05): the transport gap above is closed.** New `services/ws6-inventory/bus_consumer.py` consumes `assets.updates` (a topic `contracts/bus-topics.md` has named WS-6 as the consumer of since Phase 0, and `requirements.txt` has carried the dependency comment for since F1 -- neither was ever implemented before now) and republishes a baseline-gated first-sighting onto `raw.events` in the shape `inventory_diff`'s parser expects. `redis` is opt-in in WS-6's image now (`Dockerfile` copies `services/shared` and runs `pip install`, gated behind `BUS_BACKEND` at the `__main__` level so the zero-infra HTTP-only path -- and every existing WS-6 test -- imports nothing new; verified live: `shared` does not enter `sys.modules` when `BUS_BACKEND` is unset). New `services/ws6-inventory/test_bus_consumer.py` proves the full loop zero-infra (`BUS_BACKEND=memory`): a new device's observation republishes to `raw.events`, and that message is round-tripped through the REAL `InventoryDiffParser` into a valid OCSF event (not asserted by shape resemblance); also covers same-device dedup, cold-start-inside-baseline silence, and a plain (non-OT) observation NOT being fabricated as `sector=ot` (severity must not be wrongly escalated for a device never actually seen on an OT segment). `contracts/bus-topics.md`'s `raw.events` row updated to name WS-6 as a second producer. Rule promoted `status: stable` accordingly -- ~~**still NOT live-verified against a real Docker/Redis stack**~~ **LIVE-VERIFIED END TO END 2026-08-11** (`tools/ot_new_device_e2e.py`): a real `assets.updates` observation traversed WS-6's bus consumer → `raw.events` → WS-2's `InventoryDiffParser` → the WS-4 rule → an indexed alert on a live Docker/Redis/OpenSearch stack. **First time this rule has fired from a real producer rather than its own anti-dormancy fixture**, which closes the "the only thing that has ever fired this rule is its own fixture" caveat above. Two preconditions the test enforces rather than assumes, both learned by getting them wrong first: the entry point is the `assets.updates` TOPIC, not `POST /assets/upsert` (the HTTP route reports `new_device` in its response but republishes nothing — only `bus_consumer.make_handler` produces to `raw.events`), and it needs `INVENTORY_BASELINE_SECONDS=0` plus a tenant with no prior baseline row, since a tenant's baseline window is written once on first observation and never revised. `INVENTORY_BASELINE_SECONDS` was a documented tunable that `infra/docker-compose.yml` never exposed — added in the same pass. Full `bash run_all_tests.sh` green with this change included; `ruff`/`mypy` clean. |
| M7 Track Y + F4/HA: live Docker/Redis verification (2026-08-05) | The rows above proved the OT inventory-diff pipeline and Sentinel HA zero-infra only; this pass actually ran both against real Docker/Redis end to end, per the standing "proven = ran for real" bar in §2. **OT inventory-diff pipeline: PROVEN LIVE.** Injected a real `assets.updates` message on the live stack; traced it through every hop with real tools (not logs alone) -- `XADD` into Redis, `XINFO GROUPS` confirming WS-6's consumer read+acked it, the resulting `raw.events` message inspected directly, `normalized.events` showing a valid parsed OCSF event (`class_uid:4001`, `severity_id:4`), and the `alerts` stream showing a real fired alert ("triggered: New OT device appeared on the OT inventory segment", AI-triaged, score 70), confirmed indexed and queryable in OpenSearch. This is the first time this feature has run against anything other than the memory bus. **Found and fixed 3 real bugs live testing surfaced, none catchable by the zero-infra suite:** (1) `infra/docker-compose.yml`'s `ws6-inventory` service was never given `BUS_BACKEND=redis`/`REDIS_URL`/`depends_on: redis` -- the bus consumer code was correct but had literally never been wired into the deployable stack; (2) a pre-existing `inventory-data` Docker volume was still owned `root:root` from before `USER app` was added to the Dockerfile months ago, so the container crash-looped on `sqlite3.OperationalError: attempt to write a readonly database` the moment it tried to actually write -- fixed by re-chowning the volume, a real deployment-hygiene gap for any operator upgrading in place; (3) `docker compose up -d` without `--build` silently ran WS-2 on a stale pre-inventory_diff image, dead-lettering every notification with `no parser for source_type='inventory_diff'` -- not a code bug, but the first cut of this feature had never actually been exercised through a rebuilt image before now. **Sentinel HA: partially proven live, one real unresolved gap.** Bringing `make ha-up` up from a clean state (never done before this pass) surfaced three real, independent bugs in `infra/docker-compose.ha.yml` itself, each masked by the previous one until fixed: (1) `profiles: [ha]` on the single-instance disable list did the OPPOSITE of disable -- Compose activates a service when a listed profile is requested, so `--profile ha up` (what `make ha-up` runs) started the plain `redis`/`opensearch`/`dashboards` ALONGSIDE their HA replacements, confirmed via `docker ps` showing both `siem-bus` and `siem-bus-ha-1` `Up (healthy)` simultaneously; fixed by retagging them `[standard]`, a profile `--profile ha` never requests. (2) Fixing that exposed a second bug: Compose *merges* `depends_on` maps across `-f` files by key rather than replacing them, so every app service still depended on the now-correctly-excluded plain `redis`/`opensearch`, making the whole stack fail to start ("depends on undefined service"); fixed with the Compose `!override` merge tag (verified empirically in an isolated 3-service reproduction after `!reset` -- the first tag tried, going only on name-recollection -- turned out to silently drop the key to empty instead of replacing it, confirmed via the same reproduction before committing to `!override`). (3) Fixing that exposed a third: none of the app-service HA overrides (`ws1`..`ws6`) ever added the `ha` Docker network to their `networks:` list, so despite correct `depends_on`/env wiring they had zero network reachability to Sentinel/redis-1..3/opensearch-1..3 -- confirmed via `docker inspect` showing the app containers on `infra_default` while every HA container sat on `infra_ha`, and confirmed live inside a container that `Sentinel.discover_master()` failed with a plain network `ConnectionError` to all three sentinel hosts. Also found and fixed: `docker-compose.ha.yml` had no WS-6 override at all (added same session as the bus consumer, but the HA profile was never updated for it) -- WS-6 would have depended on the excluded `redis` and, even if not, its bus consumer would have used the wrong backend. After all four fixes, all 6 app services genuinely reported `healthy`, Sentinel correctly reported the real primary/2 replicas/quorum 2, and a real `XADD`/upsert round-tripped successfully against the Sentinel-discovered master. **Confirmed still unaffected: a plain `docker compose up` (ha.yml not included) starts the identical 10-container set as before every one of these fixes** -- re-verified after each change, not just claimed. **What did NOT get proven, and is NOT fixed**: killed the live primary (`docker kill siem-bus-ha-1`) to test the actual failover this profile exists for. Sentinel never completed it -- `SENTINEL master mymaster` kept reporting the dead container as `flags: master` for the full observation window (multiple minutes), and Sentinel's own logs showed it repeatedly entering `+tilt` mode in response to `Failed to resolve hostname 'redis-1'` (Docker's embedded DNS stops resolving a stopped container's hostname entirely, rather than the "IP unreachable" failure mode Sentinel's design assumes), apparently re-triggering tilt faster than it could clear and complete an election. `SENTINEL ckquorum` still reported the 3 sentinels healthy and quorum-reachable throughout -- this is specifically a Sentinel/Docker-DNS interaction bug in the failure-detection path, not a quorum or connectivity problem. Restarting the killed container let Sentinel quietly re-sync to it as master with no failover ever having occurred, rather than the intended replica promotion. **This means the HA profile's core value proposition -- automatic failover -- is unverified and, as currently configured, appears broken** in this Docker Desktop/Windows environment; the topology, wiring, and steady-state operation are now genuinely proven live, but automatic recovery from a primary failure is not, and needs its own follow-up (candidates: pin Sentinel to static IPs instead of hostnames, tune `down-after-milliseconds`/tilt sensitivity, or test on a non-Docker-Desktop host to rule out a Windows-specific DNS-teardown timing issue before assuming the config itself is wrong). `infra/docker-compose.yml` and `infra/docker-compose.ha.yml` both updated with the fixes and the reasoning inline, dated 2026-08-05. |
| Sentinel failover: root-caused and FIXED; one new open bug found (2026-08-05, later same day) | The row above closed with automatic failover "unverified and, as currently configured, appears broken," and listed static-IP pinning as one of three candidate follow-ups. This pass root-caused it by measurement and fixed it. **Root cause (measured, not inferred):** Sentinel monitored the primary by the hostname `redis-1` with `sentinel resolve-hostnames yes`, so it re-resolved that name on its single-threaded event loop on every health check via a BLOCKING `getaddrinfo()`. Once the primary container dies, Docker's embedded DNS (127.0.0.11) no longer holds the name and forwards the query upstream, where it takes the stock `resolv.conf` timeout to return NXDOMAIN. Timed from inside a live Sentinel container: a live name resolves in **0.00s**, a dead/unknown name takes **5.06s**. Sentinel's TILT trigger is a hardcoded 2000ms (`SENTINEL_TILT_TRIGGER` in `sentinel.c`, a compile-time constant with no config knob), so every probe of the dead primary tripped it. TILT suspends the failover state machine for 30s; on exit Sentinel retried the same dead hostname, blocked 5s again, and re-tilted — the perpetual loop the previous row observed. Reproduced exactly before fixing: `+sdown master` reached, `+odown` **never**, zero promotions, both survivors stuck at `master_link_status:down`, `SENTINEL ckquorum` reporting OK throughout (which is what isolates it to the detection path). **Correction to the previous row's speculation:** this is NOT a Docker Desktop or Windows-specific quirk. The 5s is the stock resolv.conf upstream timeout, so any Docker host resolves a dead container name the same way; the "test on a non-Docker-Desktop host" candidate was a red herring. **Fix:** the `ha` network now declares an explicit `172.28.0.0/24` subnet and pins Redis to .11/.12/.13 and the Sentinels to .21/.22/.23; `redis-sentinel-entrypoint.sh` sets `resolve-hostnames no`/`announce-hostnames no` and resolves `REDIS_SENTINEL_PRIMARY_HOST` to an IP **once, in the shell, before Sentinel starts** (blocking there is harmless), so Sentinel is only ever handed a literal IP and never calls the resolver. A hostname is still accepted for backward compatibility. **Result, verified live across three separate kill tests:** `+sdown` → `+odown #quorum 2/2` → `+try-failover` → `+switch-master` completing in **~1s**, replicas reconfigured to the new primary, the killed node automatically rejoining as a replica on restart with post-failover writes replicated to it, and all three Sentinels independently agreeing on the new master. Zero `+tilt` events, zero `Failed to resolve hostname`. **Second real bug found in the same pass:** Sentinel's leader election is Raft-like and CAN split — measured live, all three Sentinels saw `+odown` within ~80ms of each other, each voted for itself, none reached the 2-vote majority, and the round died with `-failover-abort-not-elected`. That is normal protocol behaviour, not a misconfiguration, but Sentinel gates the retry at **2x `failover-timeout`**, so the shipped 60000 meant a single split vote cost **~2 minutes** of write-unavailability before the next round (which then elected a leader and finished the switch in 0.7s). Lowered to 20000 (both it and `down-after-milliseconds` are now env-overridable), capping the split-vote penalty at ~40s while keeping wide margin over the measured sub-3s failover. **Application layer: PROVEN, and this corrects an intermediate claim made during this same pass.** A real client (`_RedisSentinelBus` on the actual `infra-ws1-collectors` image, on the `ha` network, producing every 0.3s) was run through a mid-run primary kill. A first run appeared to show the client never recovering — that read was WRONG, and the error was mine: the test window ended before Redis had finished failing over, so the client was blamed for a failover that had not happened yet. Re-run with a window that outlasts a worst-case split vote, the result is unambiguous: **1-2 failed produces, automatic recovery in ~10s** (`MASTER CHANGED -> <new primary>`), and exact message accounting (343 produced / 343 in the stream; an earlier run's 812 = 75 from a prior run + 737 from that run). `_with_failover`/`_refresh_master` work as designed. An earlier "LOST 75 messages" line from the harness was also false — `_RedisBus.depth()` swallows errors and returns 0, so the harness was reporting an unreachable dead master as an empty stream; all 75 were present on both survivors. **Bug found in this pass's own fix, before shipping:** the long rationale comment was first written INSIDE the entrypoint's unquoted heredoc, so the shell command-substituted a backticked Sentinel event name out of the prose and logged `-failover-abort-not-elected: not found` on every boot. Caught by reading the boot log rather than assuming a clean start; rationale moved out to shell comments, generated `sentinel.conf` re-inspected and confirmed clean. **NEW OPEN BUG (not fixed — needs a contract decision):** re-verifying the default stack surfaced that WS-6's now-live `assets.updates` consumer **drops 3 of the 5 observations WS-1 seeds at startup**, every time, by construction. `services/ws1-collectors/collectors/syslog_collector.py` hardcodes `"mac": None` (correctly noting syslog headers carry no MAC) and emits anyway; `InventoryStore.upsert_with_diff()` returns `None` for any observation without one (`# inventory is MAC-keyed (Contract C)`), and `contracts/bus-topics.md` confirms `mac` is the topic's partition key. So **every syslog-sourced asset observation is structurally guaranteed to be discarded** — logged live as `assets.updates observation missing mac, dropped`. The consumer itself is fine: proven by injecting a MAC-bearing observation, which upserted correctly (`entries-read` 5→6, asset visible on `GET /assets`). `netflow_collector.py` already handles the identical situation correctly by NOT emitting. This was latent until the previous row wired `BUS_BACKEND=redis` into WS-6 — nothing consumed the topic before, so the dead path was invisible. **`asset_observations()` has zero test coverage anywhere in the suite**, which is why it went unnoticed. Not fixed here because the right answer is a product/contract call, not a mechanical one: (a) make syslog abstain like netflow — minimal, contract-clean, loses no working capability since these are 100% dropped today; (b) let WS-6 resolve a macless observation onto an existing asset by hostname, enriching known devices with fresh IP sightings — genuinely useful, but a real feature; (c) re-key inventory off MAC — a Contract C change with schema, dedup-identity and alerting consequences. **RESOLVED same day: option (a) taken, plus test coverage.** `syslog_collector.handle_line()` no longer emits an asset observation at all (the parsed hostname still rides on the raw event's `meta`, it is just not used as an asset identity) — matching `netflow_collector`'s long-standing abstention for the same reason. **A second, latent instance of the identical defect was found while writing the tests and fixed in the same pass:** `snmp_collector`'s guard was `if ip != "0.0.0.0" and (mac or hostname)`, so a device answering sysName but NOT ifPhysAddress would also emit a macless observation. The shipped mock devices all carry a MAC, so it never fired on the seeded path and nothing had ever caught it; the guard now requires `mac`. **Root cause of the whole class was missing coverage, so that is what got closed:** new `services/ws1-collectors/test_asset_observations.py` (8 cases, wired into `run_all_tests.sh`) pins the invariant *a collector may only yield an `assets.updates` observation when it has a MAC* — syslog abstains with a named host and with an IP-literal host, snmp emits with a MAC and abstains without one, netflow emits nothing, and over a full seeded `run_once()` **every** published observation carries a MAC and its partition key **is** that MAC. The previously-existing `test_contract.py` asserted only that at least ONE observation had a MAC, which stayed true while every syslog-sourced one was unusable — that is the precise hole this closes. The new test was verified non-vacuous by temporarily restoring the old emission: it fails with 7 problems naming the exact three hostnames (`fw-edge-01`, `wks-jdoe`, `db-oracle-02`) observed being dropped on the live stack, then passes again once reverted. `collectors/__init__.py`'s interface docstring now states the MAC requirement as part of the collector contract. Follow-up from this branch's own code review: `SyslogCollector.asset_observations()` was left as a drain loop over a `self._assets` list that, once the emission was removed, nothing ever appended to — live-looking machinery that reads as a working buffer and invites the next person adding an OT or DHCP-derived observation to append to it, silently reintroducing exactly the macless emission the contract now forbids. Both the accumulator and the loop are gone; it is now `return iter(())`, byte-identical in form to `netflow_collector`'s long-standing abstention, which makes the behaviour structural rather than incidental. No behaviour change (it already yielded nothing) — the tests above and the full suite are unchanged and green. **(b1) remains open and is deliberately NOT built here** — enriching an already-known asset from a macless sighting by matching the observation's IP against `ip_history` through WS-6's existing `InventoryStore.resolve()` (already tenant-scoped, time-aware and index-backed via `idx_ip_history_tenant_ip`). Tracked as a real follow-up feature, not a bug: it needs its own handling of DHCP/NAT address reuse, whose failure mode is silently attaching a hostname to the wrong device, and it carries an honest ceiling — `ip_history` is only populated by MAC-bearing upserts, so it can only ever enrich devices SNMP already discovered, and contributes nothing in a deployment without SNMP. (c) is not planned unless a real SNMP-less deployment forces it. **Code review of this pass found two more real bugs IN THE FIX, both fixed and both verified against the daemon rather than argued about.** (1) The new `ha` ipam declared only `subnet: 172.28.0.0/24` with no `ip_range`, and Docker allocates dynamic addresses sequentially from .2 straight through the pinned .11-.13/.21-.23 block — this profile puts 10 ha-attached containers with no `ipv4_address` on that network (opensearch-1..3, dashboards-ha, ws1..ws6), which is enough to reach .11. Reproduced: 10 dynamic containers on a bare /24 take .2-.11, after which `docker run --ip 172.28.0.11` fails `Address already in use`. `make ha-up` survives on start ordering alone (redis-1 has no `depends_on`, the app services all wait on it), which is why the isolated 6-container Redis testbed used for the failover work never hit it — but a partial `up`, a `--force-recreate` of redis-1 while the rest run, or one more ha-attached service makes it a hard startup failure. Fixed with `ip_range: 172.28.0.128/25`, verified: 12 dynamic containers now start at .129 and all four spot-checked static addresses stay acquirable. Note the range must be CIDR-aligned — the daemon **silently normalizes** a misaligned one instead of rejecting it (`172.28.0.24/27` → `172.28.0.0/27`, which would confine every dynamic into .0-.31 and *guarantee* the collision; confirmed against the daemon, and worth recording because it is exactly the shape of fix that looks right and does the opposite). (2) The entrypoint's resolve loop was bounded on an iteration count (30 × getent + `sleep 1`) and reported "after 30s", but an unresolvable name is precisely the case where getent blocks for the resolver timeout — the same stall this script exists to keep off Sentinel's event loop. Measured end to end: **179s** before failing with a message understating it 6x, pointing diagnosis away from DNS. Now bounded on elapsed wall-clock (`REDIS_SENTINEL_RESOLVE_TIMEOUT_S`, default 60) and reports real elapsed; re-measured at 67s, the ~7s overshoot being one in-flight lookup, which the message now states. Also confirmed in the same pass: the backward-compatible hostname branch of the entrypoint (`REDIS_SENTINEL_PRIMARY_HOST` set to a name rather than an IP) had never been executed by any of the failover tests, since all of them passed a literal IP — exercised directly and confirmed to resolve and emit a valid config. **Default stack re-verified live after every change**: plain `docker compose up --build` brings up all 12 services healthy, `docker compose config` on the base file alone shows **zero** references to `172.28.0.*`/`sentinel`/`resolve-hostnames` (no HA leakage), the one-shot `provision`/`devkit-feeder` containers exit 0, and the seeded SSH brute-force burst still produces real indexed alerts in OpenSearch (`ai-ssh-198.51.100.23-9`, level `high`, AI verdict `suspicious`, score 70). Full `bash run_all_tests.sh` green with all changes included; all Docker infrastructure torn down afterward. |
| Full HA profile brought up for the first time — 3-node OpenSearch proven, two more real bugs found (2026-08-05) | Every prior HA pass used an isolated 6-container Redis/Sentinel testbed; the complete 18-container profile had **never** been started. Doing so immediately confirmed the `ip_range` fix at real scale (statics landed on .11-.13/.21-.23, all 10 dynamic containers confined to .129-.138, no `Address already in use`) and proved the profile-exclusion fix holds with the whole stack up: no `siem-bus`, `siem-store`, or `devkit-feeder` alongside their HA replacements. **OpenSearch 3-node cluster: PROVEN, previously listed as unverified.** `status: green`, 3 nodes / 3 data nodes, 0 unassigned shards — which also empirically confirms the ha-design doc's prediction that the existing templates' `number_of_replicas: 1` starts actually assigning on a real multi-node cluster with zero application-code change (single-node shows yellow with the replica unassigned; this is the first time that claim was tested rather than reasoned about). Full pipeline proven end to end on HA: an injected SSH brute-force burst produced 12 indexed events plus 4 alerts, every index green with its replica assigned, and the alert carried real WS-5 triage. **Bug 5 — `provision-ha` had no `networks:` key at all**, so it sat on the base file's `default` network while opensearch-1..3 live on `ha`. It could not resolve `opensearch-1`, hung forever on provision.sh's wait loop, and installed nothing: confirmed against the live cluster as **0 index templates, 0 ISM policies**. An HA deployment was therefore indexing into whatever dynamic mappings OpenSearch inferred, with no retention policy whatsoever, instead of the contract mappings. Same defect class as the app-service network fix documented above, and missed by it because `provision-ha` is an HA-only service defined in this file rather than an override of a base-file service, so it was never in the set of overrides being audited — `depends_on: opensearch-1 service_healthy` was satisfied the entire time, which again only proves the target is healthy, not that this container can route to it. Fixed with `networks: [ha]`; provision-ha now exits 0 with "Provisioning complete." and the cluster reports 5 index templates (all `replicas=1`) and 5 ISM policies. **Bug 6 — the application tier did NOT survive failover, and this one invalidates an earlier claim in the row above.** Killing the primary under the full stack: Redis-side failover completed in **1.2s** (`+sdown` → `+odown 3/2` → `+switch-master`, no tilt, no split vote), but ws2/ws3/ws4/ws5 all went `unhealthy` looping on `redis.exceptions.ConnectionError: Error 113 connecting to <dead primary>:6379. No route to host.`, with 12 messages stranded at `lag=12` on the promoted primary. Root cause in `services/shared/bus.py`: `_RedisBus.consume` and `_RedisBus.claim_pending` are **generator functions**, so `getattr(self._bus, method)(*args)` inside `_with_failover` merely constructs a generator and performs no I/O — nothing can raise there, the `except` is unreachable, and `_refresh_master()` was never called on the two methods every long-running service spends its entire life in. The ConnectionError surfaces later, when the runner iterates, outside the try; the runner's retry then built another generator against the same stale `self._bus`, pinning the service to the dead primary permanently. The earlier row's "**Application layer: PROVEN**" was proven for `produce()` ONLY — `produce`/`ack`/`depth`/`lag`/`trim_acked` are ordinary functions and genuinely were covered, which is exactly why a produce-only synthetic client recovered cleanly in the same scenario and made this invisible. Fixed with a separate `_iter_with_failover` that delegates via `yield from`, putting the iteration (where the I/O actually happens) inside the try; a retry may redeliver entries the first generator already yielded, which is consistent with this bus's documented at-least-once contract and strictly better than wedging. **Verified live, the strong way**: after rebuilding, the primary was killed again and NO service was restarted afterward — a third distinct attacker injected at the promoted primary flowed all the way through to an indexed, AI-triaged alert (`ai-ssh-192.0.2.66-9/-10/-11`), consumer groups back to `lag=0`, all services still healthy on their original processes. New zero-infra `services/shared/test_sentinel_failover.py` (5 cases, wired into `run_all_tests.sh`) pins it with fakes: consume/claim_pending must rediscover on ITERATION rather than on call, plain methods must keep failing over, and a structural case asserts that whichever `_RedisBus` methods are generator functions are exactly the ones `_RedisSentinelBus` returns generators for — so a future method turned into a generator fails loudly instead of silently losing failover. Verified non-vacuous by restoring the broken wrapper: the same `ConnectionError` escapes and the test fails. **One unresolved observation, recorded as inferred rather than proven:** `bash run_all_tests.sh` failed once and then passed on three consecutive re-runs after the Docker stack was torn down. The likely cause is host-port contention with the running stack (WS-1 publishes 5514/udp, WS-6 8000, WS-7 8080) against the tests that bind ports, but the specific failing test was NOT isolated — recorded here rather than dismissed. It implies the zero-infra gate is not actually safe to run while a live stack is up, which contradicts how CLAUDE.md presents it. |
| HA: Sentinel state persistence + single-source primary address (2026-08-05) | Closes the last two review findings on this branch, both verified live rather than argued. **Sentinel state now persists.** A `sentinel.conf` is not an input file, it is Sentinel's persistent STATE — it CONFIG REWRITEs its `myid`, every discovered `known-sentinel`/`known-replica`, its epoch, and the current master back into whichever file it was started with. The entrypoint wrote to `/tmp/sentinel.conf` and overwrote it unconditionally on every boot, while `docker-compose.ha.yml` mounted a named volume at `/data` that consequently held nothing whatsoever. Every restart handed Sentinel a fresh identity and reset it to the template's ORIGINAL primary. One Sentinel restarting re-learns the true master from its peers over the hello channel and self-corrects, which is why this hid — but restart all three together after a failover and no surviving peer exists to correct them. Now written to `/data/sentinel.conf` (mode 600 — `sentinel auth-pass` puts REDIS_PASSWORD in the file, so the volume is secret-bearing and that is stated in the script) and generated ONLY when absent, with `REDIS_SENTINEL_FORCE_RECONFIG=1` to regenerate when the tunables genuinely change; `make ha-down` already runs `down -v`, so a clean teardown resets state without the flag. **Proven live on exactly the failure scenario**: brought the quorum up, killed the primary, watched Sentinel promote 172.28.0.13, then restarted all three Sentinels simultaneously — all three came back with byte-identical `myid`s and the quorum resumed on the **promoted** 172.28.0.13, not the original .11. Before the fix the same sequence produced three new identities pointed back at .11. The force-reconfig path was exercised separately and correctly regenerated with a new myid. **Primary address is now a single source of truth.** `REDIS_PRIMARY_IP` (default 172.28.0.11) is interpolated into redis-1's `ipv4_address`, both replicas' `--replicaof`, and all three Sentinels' `REDIS_SENTINEL_PRIMARY_HOST` — previously the address was written out literally in five places with nothing keeping them in sync, and the one knob the file advertised (`REDIS_SENTINEL_PRIMARY_HOST`) moved only the Sentinels. An operator using it got a monitored primary whose replicas were still trying to replicate from the hardcoded .11, i.e. a master with zero promotion candidates — Sentinel cannot fail over to nothing, so the advertised knob silently defeated the very guarantee the profile exists for. Verified: the default resolves to .11 in all five places, and `REDIS_PRIMARY_IP=172.28.0.31` propagates to all six occurrences in the resolved config. Full `bash run_all_tests.sh` green; all Docker state torn down. |
| PR / merge status | **PR #2** (`github.com/supermhel/fengarde/pull/2`, branch `claude/repo-status-plan-ehbr6k` → `main`) carried all of M1-M6 + the adversarial bug-hunt fixes above — 31 commits, 185 files, +10877/-391. **Merged 2026-07-18** after semantic conflict resolution across 14 files (`Makefile`, `CHANGELOG.md`, `SSOT.md`, `base.py`, `active_directory.py`, `cisco_asa.py`, `db_audit.py`, `generic_syslog.py`, `linux_ssh.py`, `mcp_agent.py`, `n8n_audit.py`, `opcua_audit.py`, `vmware_vsphere.py`, `windows_eventlog.py`) — `main`'s independent P2.x hardening series (cross-source severity rubric / `sector` param) and this branch's envelope-v1 work (`meta` param, tenant propagation) both touched `base_event()`'s call sites; resolution kept both params/both call-site kwargs everywhere rather than picking a side, since neither superseded the other. `services/ws4-detection/main.py` (the tenant-isolation-sensitive file) auto-merged cleanly with zero conflicts. Full test suite run post-merge before landing — see `run_all_tests.sh` output in the merge commit. |
| Security-hardening pass: independent audit + unified fix (2026-08-06, commits `61cc906`..`c279482`+`32d17f8`+`ba1511f`, 11 commits) | An independent security/architecture/code-quality audit (`independent_security_audit_2026-08-06.md`, 28 findings) was cross-checked against a prior swarm-agent review (`swarm_review_report.md`) and a dedicated architecture/HA subagent audit (`review_architecture_ha_audit.md`); `report_comparison_and_enhancements_2026-08-06.md` reconciles the two independent takes, and both were merged into one `unified_fix_plan_2026-08-06.md` (39 findings + 20 enhancements). A separate `github_actions_audit_2026-08-06.md` covers the CI/CD pipeline specifically. Fixes executed via parallel structured agents with independent orchestrator re-verification of every change, recorded in `implementation_report_2026-08-06.md`. **Detection-integrity (`27b4e54`, CRITICAL/HIGH)**: HA `BUS_BACKEND` env-gate now accepts `redis-sentinel` (not just exact-match `redis`), un-breaking 12 stateful rules that were silently falling back to per-process window counters under Sentinel; poison-pill guard on `window_seconds`/`threshold` (load-time type validation in `Rule.__init__` + a runtime fail-closed wrapper around the stateful-eval path); `Detector.process()`'s `class_uid=None` catch-all bucket was being evaluated twice for a classless event; `db_audit.py`'s substring-match `_OP_MAP` reordered privilege-first so `GRANT SELECT` stops misclassifying as a read; `shared/ocsf.py::valid_ip` now normalizes IPv4-mapped IPv6 (`::ffff:a.b.c.d`); `inventory_diff.py`'s 1000x time-unit bug. **Session/auth (`d2cb9d5`)**: HMAC-SHA256 signing of Redis-held session rows (FIX 5, `services/shared/sessions.py`); SSRF hardening via a no-redirect `urllib` opener (`services/shared/outbound_http.py` — renamed from the illegal `shared/http.py`, which shadowed the stdlib `http` module and silently broke `import urllib.request`, a real latent bug caught in the same pass); `FENGARDE_REQUIRE_AUTH` boot-time gate (`services/shared/authz.py::require_auth_or_die`) that refuses to start with an incomplete auth surface. **OpenSearch + audit log (`f966881`)**: `OpenSearchStore` accepts a comma-separated node list and round-robins to a surviving node on connection failure (FIX H6); `services/ws3-indexer/audit.py` — append-only, admin-scoped, capacity-capped, fail-open JSONL audit trail wired into login/triage/report. **Opt-in MFA/TOTP (`969fb8a`, E3)**: `services/ws6-inventory/mfa.py`, stdlib-only RFC 6238; `services/shared/users.py` gains additive `totp_secret`/`totp_active` columns; login gates on `totp_code` once a user's TOTP is active. **WS-1 (`0732f22`)**: per-source syslog `/metrics` breakdown (bounded map, LRU-evicted, thread-safe); counters moved under the existing `_shed_lock` (were racy across the P0-4 worker pool). **Dashboard UX (`3b550e5`, E11/E12/E13)**: saved alert-search filters (client-side localStorage), dark/light theme toggle (OS-preference fallback), alert-lifecycle guidance + per-rule playbook rendering. **CI/supply-chain (`3795ffc`)**: removed `\|\| true` from 5 Dockerfiles' `pip install` (a broken requirements.txt now fails the build instead of shipping a runnable-but-broken image); live-Redis session-store + window-counter CI jobs added; live-OpenSearch integration CI job added; Sigma regex-to-glob narrowing fix (M18) for the fully-literal translation branch. **Durability + docs (`fec580c`, `c279482`, `32d17f8`, `ba1511f`)**: Redis HA primary now runs `--min-replicas-to-write 1 --min-replicas-max-lag 10` (FIX 23 — see §2's chaos-gate row for the honest scope note this forced); `SECURITY.md` gained sections on the Grafana default credential, the empty-by-default `FENGARDE_API_KEY_PEPPER`, and webhook-secret sourcing. **All of the above is zero-infra proven** (`run_all_tests.sh`, ruff, mypy all green per `implementation_report_2026-08-06.md`) — none of it was live-Docker-verified by this pass itself; see the next row for what an independent review then found in it. |
| Independent review of the pass above + fix-response (2026-08-06, commit `983efc7`, same PR #54) | A second independent pass reviewed the 11-commit hardening pass above from scratch (not trusting `implementation_report_2026-08-06.md`'s self-reported "all green" — re-ran the full gate, then read the actual diffs). It confirmed one specific adjudication the implementation report flagged for scrutiny — an earlier agent had flipped the `not_in` allowlist operator to fail CLOSED on a missing/malformed allowlist file, which the report's author reverted back to fail OPEN and asked a reviewer to sanity-check — and found the revert correct: `git diff` against `test_v03_rule_grammar.py`/`test_v04_rule_tuning.py`/`test_v05_agent_rules.py` (all pre-existing, untouched by this branch) confirms fail-open on a broken allowlist FILE (rule keeps firing = noise, never a missed detection) was already the project's tested intent, not something invented to justify the revert. The review then found **1 CRITICAL regression and 4 HIGH-severity issues newly introduced by the same pass**, all fixed here: **CRITICAL — FIX 21 silently broke threshold detection.** `services/ws1-collectors/collectors/syslog_udp_server.py::_handle_datagram` had been hardcoded to `deterministic_id=True` on the theory that "UDP retransmission is normal" (false — UDP has no retransmission mechanism; duplicate datagrams are rare network-level duplication, while genuinely-repeated identical log lines, e.g. N separate brute-force attempts logging the same "Failed password" text, are common). Content-hashing collapsed every such repeat to ONE `meta.ingest_id`, and WS-4's window counters dedup by that id (`member in members: count = len(w)`, `window.py`), so N real attempts counted as 1 and a threshold rule never fired — proof-of-concept: 6 identical repeated lines through the real server + real `DequeWindowCounter` produced window counts `[1,1,1,1,1,1]` (never fires) before the fix, `[1,2,3,4,5,6]` (fires) after. Reverted to honoring the constructor's `deterministic_id` flag (default `False`); the regression test that had enshrined the broken behavior (`test_fix_counters_deterministic.py`) was rewritten to assert the flag is honored in BOTH directions instead. **HIGH — session-signing forgery bypass + cross-replica breakage.** `RedisSessionStore.resolve()` treated a signature-free row as backward-compatible and accepted it unconditionally — exactly the "any process that can write to Redis can forge a session" threat FIX 5's own docstring named, just reopened via an unsigned row. Separately, an unset `FENGARDE_SESSION_SECRET` silently generated a random PER-PROCESS fallback key, which defeats cross-replica signing entirely (each replica signs with a key no other replica has, so every replica's own sessions look forged to every other replica) while only warning, never failing. Fixed: `RedisSessionStore.__init__` now raises `RuntimeError` if the secret is unset (fail loud, same posture as the pre-existing unreachable-Redis case); `resolve()` requires a valid `sig` unconditionally, no unsigned path. **Live-verified against a real (throwaway, local) Redis container**: construction correctly refuses without the secret; a forged/unsigned row is rejected; a legitimately-signed session resolves; and — the actual point of FIX 5 — two independent client instances configured with the same secret now genuinely agree on each other's signed sessions, which the random-per-process fallback had silently broken. **HIGH — MFA disable-by-cookie-theft.** `POST /auth/mfa/enable` required only a valid session cookie; `enable_totp()` unconditionally resets `totp_active` to 0, so a stolen cookie alone could disarm an account's MFA with one request, and the response handed the attacker the new provisioning secret too. Fixed: both `/auth/mfa/enable` and `/auth/mfa/verify` now require the acting user's own current password (`_mfa_reauth`, `services/ws3-indexer/triage_api.py`), rate-limited in a separate `mfa:` namespace (reuses `LoginRateLimiter`), every outcome audited. **HIGH — Sentinel client pinned to a stale demoted master.** `services/ws4-detection/main.py` resolved the Redis Sentinel master ONCE at boot via `discover_master()` and built a fixed `redis.Redis(host, port)` — on a real failover the client kept writing to the now-read-only demoted node until process restart, the exact event Sentinel exists to survive. Fixed: `Sentinel.master_for()`, which re-resolves the current master on every reconnect. **HIGH — H6 OpenSearch failover was wired to one node.** `infra/docker-compose.ha.yml` set `OPENSEARCH_URL` to `opensearch-1` only, so the multi-node round-robin code shipped in the same pass was dead in the one compose profile that exists to exercise it. Fixed: all 3 nodes comma-separated, `depends_on` extended to all 3; `OpenSearchStore`'s node-selection + connection-lifecycle state (shared across ws3-indexer's 4 concurrent topic-consumer threads) is now guarded by a lock that does not hold across the actual blocking socket I/O. **Also fixed, lower severity**: a self-contradicting CI change (mutmut flipped to "blocking" while its own config comment still said "informational," and `[tool.mutmut]` has no threshold field for anything to actually gate on — reverted, `\|\| true` restored); the Sigma `.*`-wildcard-branch narrowing bug (M18 had only fixed the fully-literal branch; `^cmd.exe .*payload$` still translated to the glob `cmd.exe *payload`, silently narrowing "any char" to a literal dot) — closed on both branches now, both sigma test files also wired into `run_all_tests.sh` for the first time (they existed but were never CI-gated); and a bonus finding made while re-reading the `not_in` fix — the module-level `_ALLOWLIST_CACHE` (`engine.py`) was never invalidated, so an operator fixing a broken allowlist file would see the fix ignored until process restart, closed by clearing the affected cache entries at the start of every `load_rules()` pass. **Independent review chain**: Codex (first choice per project convention) was unavailable (usage-limit, resets 2026-09-03); fell back to the `cavecrew-reviewer` subagent per the documented policy, which read the actual diffs/files (not just the implementation report's claims) and returned an explicit per-finding verdict. Full gate re-run green after all fixes (`run_all_tests.sh`, ruff, mypy across all 8 workstreams); `docker compose -f infra/docker-compose.yml -f infra/docker-compose.ha.yml --profile ha config` validated the HA wiring change. **Still zero-infra-plus-one-container proven, not full-stack live-verified**: the session-signing fix was checked against a real standalone Redis container (see above); the Sentinel `master_for()` fix, the MFA reauth flow, and the H6 multi-node lock were NOT re-run against a live failover/full-stack scenario by this pass — a real follow-up, not silently assumed proven by analogy to the earlier (different-code-path) live Sentinel failover work in the rows above. |

## 2. What's proven vs. what's still a claim

Read this before trusting any status word ("done", "proven", "resolved") in an older doc —
those words get reused loosely across specs written weeks apart.

- **Proven** = ran against real infra (real Redis, real Docker, a real adversarial subagent
  review with independent re-derivation) and the evidence is in a commit or a doc section
  with actual command output, not just a status label.
- **Design/claim** = written down as intent, not yet executed or verified. Treat as a plan,
  not a fact.

| Claim | Status | Evidence |
|---|---|---|
| Bus-only coupling (zero cross-workstream imports) | **Proven** | An audit (2026-08-13) found the "zero hits" grep had been wrong since 2026-08-07: `services/shared/users.py` reached into `services/ws6-inventory/mfa.py` via `sys.path`. Independently, PR #58 ("MFA inert in every deployed container") relocated the TOTP primitive to `services/shared/mfa.py` as a sibling module the same day, for an unrelated reason (the old cross-tree path resolved fine in a source checkout but not inside ws3-indexer's deployed container image, since its Dockerfile copies `services/shared` and not `services/ws6-inventory` — MFA was silently inert in every real deployment). Re-verified after merging that PR into this branch: `grep -rn "ws6-inventory\|ws[0-9]-" services/shared/*.py` now returns zero real imports, only doc-comment path mentions. Genuinely zero cross-workstream imports as of this merge. |
| WS-4 redelivery + DLQ on real Redis (XAUTOCLAIM/XPENDING) | **Proven** | Live-infra session, build plan §CI+live-infra |
| Deterministic `alert_id` (T7, dedup under redelivery) | **Proven** | `services/ws4-detection/engine.py`'s `Rule.alert_key()`, tested live; both branches now tenant-namespaced (P1-1, 2026-07-21) |
| 3-tier edge/local/central deployment topology | **Design only** | Owner's internal production-roadmap doc — nothing built |
| Bus xautoclaim RESP3 "incompatibility" (redis-py 8.x) | **Investigated, not real** | An adversarial review claimed redis-py 8.x under RESP3 changes `xautoclaim`'s return shape (cursor no longer at index 0), which would break PEL redelivery. Live-verified 2026-08-05 against real Redis (redis:7-alpine, redis-py 8.0.1, both RESP2 and explicit `protocol=3`): return shape is unchanged, `[next_cursor, entries, deleted_ids]`, cursor correctly at index 0 under both protocols. The original code was already correct; no fix was needed or applied. Recorded here so the same false lead isn't re-investigated. |
| F4 per-tenant throughput isolation at WS-1 ingest edge | **Proven** | `services/ws1-collectors/collectors/syslog_udp_server.py::TenantTokenBuckets` gives each tenant an independent token bucket; new tests in `services/ws1-collectors/test_syslog_udp.py` (`test_per_tenant_token_bucket_isolates_at_edge`, `TestTenantTokenBuckets`) verify isolation + bounded map growth, and the full zero-infra suite passes. Bus-level per-tenant stream isolation was attempted in the same pass but shipped with a real gap (`consume()` never read back from the per-tenant streams `produce()` wrote to, silently losing messages when enabled) and was reverted rather than shipped half-finished; edge-level rate limiting is the complete fix for this finding. |
| HA opt-in profile: Sentinel master discovery | **Proven (Redis side)** | `BUS_BACKEND=redis-sentinel` via `_RedisSentinelBus`; `infra/{docker-compose.ha.yml,redis-sentinel-entrypoint.sh}` ship 1 primary + 2 replicas + 3 Sentinels + 3-node OpenSearch. Live-verified 2026-08-05 on Docker Desktop: brought up primary + 1 Sentinel, Sentinel correctly auto-discovered both replicas and monitored the master; `_RedisSentinelBus.discover_master()` correctly resolved `redis-1:6379` and a real produce/consume round-trip completed through the Sentinel-discovered connection from a container on the same network. **Three real bugs found and fixed in this pass, none caught by zero-infra tests or code review alone**: (1) `redis-sentinel-entrypoint.sh` had CRLF line endings (Windows-authored), which crashed `/bin/sh` inside the Linux container with a cryptic `illegal option -` error; (2) the sentinel.conf template duplicated the master name (`sentinel monitor mymaster mymaster redis-1 6379 2`, 5 tokens where Sentinel expects 4) and hardcoded `mymaster` on the auth-pass/down-after/parallel-syncs/failover-timeout lines instead of using the configured name, both fatal config-parse errors; (3) missing `sentinel resolve-hostnames yes`/`announce-hostnames yes` -- Sentinel does not resolve hostnames for `sentinel monitor` by default, so a hostname target (`redis-1`) fails with "Can't resolve instance hostname" even though plain DNS resolution on the same network works fine. ~~**Not yet exercised**: an actual failover (killing the primary and confirming Sentinel promotes a replica and `_RedisSentinelBus` picks up the new master).~~ **Superseded 2026-08-05 (later same day) -- failover is now PROVEN.** It was first found genuinely broken (perpetual Sentinel `+tilt` from a 5.06s blocking DNS lookup of the dead primary's hostname on Sentinel's event loop, vs. its hardcoded 2000ms tilt trigger), then root-caused by measurement and fixed by pinning the Redis/Sentinel nodes to static IPs and taking Sentinel off the resolver entirely. Verified live across three kill tests: `+odown` → `+switch-master` in ~1s, replica promoted, old primary auto-rejoining as a replica, and a real `_RedisSentinelBus` client recovering automatically in ~10s with exact message accounting (343/343). A second real finding in the same pass -- Sentinel leader-election split votes gated behind `2x failover-timeout`, costing ~2min of write-unavailability -- was fixed by lowering `failover-timeout` 60000→20000. See the dedicated row in the table above for the full measurement chain, the corrections it forced to earlier claims, and the one NEW open bug it surfaced (syslog-sourced `assets.updates` observations being 100% dropped as macless). OpenSearch 3-node side remains unverified (compose config only; this pass deliberately scoped to the Redis/Sentinel failover path and ran the Redis quorum in isolation). |
| Kafka as central-tier bus | **Design only, contradicted by code** | No `_KafkaBus` exists; `bus.py`'s docstring and the frozen `contracts/bus-topics.md` contract both corrected (2026-07-21 — the contract had been missed in the earlier docstring fix) |
| Rule matching scales past ~50 rules | **Fixed (v0.3 B1)** | Detector buckets rules by class_uid equality selection, only candidate bucket evaluated per event (`services/ws4-detection/main.py`); byte-identical firing behavior verified |
| Every shipped rule has a real producer (no dormant rules) | **Proven** | `tools/check_rule_producers.py` in `run_all_tests.sh` — found and fixed `bank_db_priv_esc` dormancy (class 6005 had no emitting parser until the DB-audit parser) |
| Every MITRE-tagged rule's own condition/threshold logic fires (satisfiable ≠ fires) | **Proven (27/27), at the `Rule.evaluate()` level only** | `eval/attack/fire_check.py` replays each tagged rule's own producer fixture through the real `Rule.evaluate()`. It uses `Detector` as a rule loader and calls `evaluate()` directly, so it does NOT exercise `Detector.process()`'s `class_uid` prefilter or per-tenant rule-disable — a rule mis-bucketed into a `class_uid` no event carries still shows FIRED here (demonstrated in review). That routing class is `engine.py`'s own concern, not covered by this gate. |
| Rule engine agrees with each rule's DECLARED threshold/window (no off-by-one, no wider-than-declared window) | **Proven for the 12 stateful rules** | `fire_check.py`'s `under_threshold` + `window_overrun` probes, sensitivity-tested by `eval/attack/test_fire_check.py` (mutates rules until genuinely too loose and requires `main()` to exit 1 end-to-end). **Not a claim that any threshold is well CHOSEN**: the probes derive their event count from `rule.threshold`, the same declared value the engine compares against, so they prove engine-vs-declaration consistency and nothing about whether a declared threshold is too permissive for the real world — lowering a rule's declared threshold keeps this gate green. |
| Rule engine actually enforces every predicate a STATELESS rule declares | **Proven for all 15 stateless rules (46 predicate near-misses, 0 skipped, 2026-08-11)** | `fire_check.py`'s `_near_miss_probe`. Supersedes this row's previous "the 15 stateless ones are UNTESTED — no generatable near-miss, each needs a hand-authored fixture": that claim was too strong. Every shipped stateless rule's condition is a pure conjunction of field predicates, so violating exactly ONE declared predicate on the fixture that fired must silence the rule — generatable per predicate, not hand-authored. Constructors per predicate kind: perturbed value for an equality selection, one step across the boundary for `gt/gte/lt/lte/ne`, a value outside the list/needle/pattern for `in`/`contains`/`glob`, an INSIDE-business-hours timestamp for `outside_hours`, and for `not_in` the inverted case — the event is left alone and the rule is rebuilt against a throwaway allowlist dir in which its value IS listed, so the suppression path itself is exercised. **Read narrowly: single-predicate NECESSITY, not well-scopedness** — same limit the stateful row carries on declared thresholds. It proves each declared predicate is load-bearing at evaluation time, catching what the positive gate passes silently: a condition evaluated as `or` where `and` was declared, a `not_in` allowlist never consulted, an `outside_hours` window ignored (a rule that ignores time-of-day fires on its own off-hours fixture exactly like a healthy one), a compile step that drops a selection field. It says nothing about whether the declared predicate SET is the right one. Non-conjunctive conditions and predicates with no constructible violation are reported inapplicable/skipped, and any skipped predicate keeps its rule out of the "fully held" count. Sensitivity-tested end-to-end: `test_fire_check.py` drops a declared field from a rule's compiled selections (declaration intact, engine no longer checks it) and requires `main()` to exit 1, separately for a plain equality predicate and for an `outside_hours` one, plus declines on `or` conditions and on any rule whose positive control does not reproduce. |
| Triage + report workflow works through the live Docker/nginx stack | **Proven (2026-07-10, `38341ce`)** | Live run on real Docker/Redis/OpenSearch/nginx: dashboard nginx `/api/triage` + `/api/report` proxy → `ws3-indexer:8013` works; report lands in `reports-*` (status=draft+disclaimer); auth 401/200 correct; nginx injects the key server-side. |
| Triage OCC/CAS prevents lost updates under REAL concurrent writers | **Proven live (2026-08-11)** | `services/ws3-indexer/storage/test_opensearch_cas_concurrency_live.py` (in `make test-live`): 8 threads, each its own `OpenSearchStore`, race read-modify-write (`find_alert_versioned` → `index_cas`, bounded retry on 409) against one alert on a real 3-node cluster; all 8 triage notes survive, zero lost updates. Supersedes this row's previous "unproven at scale: OCC/CAS unit-tested against a fake transport only". **Sensitivity-verified, not just observed green**: re-running with `index_cas` monkeypatched to write unconditionally (the pre-OCC behavior) loses 7 of 8 notes — so the test measures concurrency control rather than passing by construction. Honest scope: exercises the STORAGE layer's CAS directly, not the triage HTTP endpoint end-to-end under load (that path adds an in-process lock the storage layer knows nothing about). Found and worked around in the same pass: `OpenSearchStore` holds a non-thread-safe urllib opener, so sharing one across threads raises `URLError(Idle)` and reads exactly like an OCC failure — each writer gets its own client, which is also the honest shape of two analysts on two replicas. |
| Opt-in live-infra test lane (`make test-live`, v0.4 P2.6) | **Proven (2026-07-16)** | `services/ws3-indexer/storage/test_opensearch_live.py` + redis-parametrized `services/shared/test_runner.py` run against a real Docker stack (Redis 7 + OpenSearch 2.13): idempotent-upsert confirmed, real 409 on stale OCC `if_seq_no`, permanent-4xx not retried, real XAUTOCLAIM/XPENDING PEL/DLQ round-trip on both memory and redis backends — both PASS, not just skip. |
| P0-P2 detection-integrity fixes hold on real infra | **Proven (2026-07-16)** | Live Docker stack, devkit-feeder produced a 12-event SSH brute-force burst against `198.51.100.23`; real alert (score 70, rule `Authentication brute-force from single source`) landed in `alerts-*` with AI triage summary attached; feeder rerun deduped via the same deterministic `alert_id` (idempotent redelivery holds on real Redis Streams, not just the memory-bus test double). |
| B3 dual-backend behavioral test (memory+redis, ack/redelivery/DLQ/poison) | **Proven live (2026-07-21)** | `BUS_BACKEND=redis python services/shared/test_runner.py` run against Docker Desktop's real Redis: all six parametrized bodies passed `[redis]` alongside `[memory]` (handler-once, fail-no-ack, redelivery→DLQ, flaky-eventually-acked, multi-topic, threaded serve+health+metrics). Confirms the CI `redis-integration` job's coverage is real, not just passing in CI's specific environment. |
| B2 backpressure protects Redis under a real flood | **Load-tested live (2026-08-11) — the broker IS bounded, but NOT by the mechanism the row used to name** | `tools/backpressure_load_test.py`: 120,000 datagrams at ~4,300/s (2.2x the 2,000/s cap) from 12 parallel senders **inside the docker network** against the live ws1 listener. Result: only ~22,000 reached the bus, ws1 stayed healthy, and every non-produced datagram was accounted for. **Measured finding that corrects the previous framing:** B2's token bucket shed *nothing* (`events_shed=0`). What actually bounded the broker was the P0-4 recv→worker queue (`events_queue_full≈85,000`) and the kernel receive buffer. The ordering explains it — `_recv_loop` does `queue.put_nowait` FIRST and counts `events_queue_full` on refusal, while the token bucket lives downstream in `_worker_loop` and only sheds for datagrams that got past the queue. So under sustained overload the **bounded queue is the flood defense and the bucket is the steady-state rate cap**, not the other way round. Both are counted, so nothing is silently lost. Also measured: a host→container flood is useless for this test — Docker Desktop's NAT drops ~75% in transit, arrivals stay under the cap, and no mechanism engages while the test still passes. **Still true:** the 2000/s rate default and 100k depth threshold remain untuned guesses; this proves the mechanism engages and accounts for its loss, not that the number is well chosen. |
| Open-core split (this repo free / fengarde-sec paid) | **Decided AND publicly documented (2026-07-21)** | README.md "Open core" section states it explicitly (free forever, paid layer only through the `contracts/reporting.md` seam); LICENSE stays plain Apache-2.0 (correct — the license needs no carve-out, the split is a repo boundary, not a license term) |
| ILM/retention policies actually enforced on a live OpenSearch cluster | **FIXED and Proven live (2026-07-21)** | Rewrote the 4 retention policies in real OpenSearch ISM schema (`contracts/opensearch-mappings/ism-events-30d.json`, `ism-events-90d.json`, `ism-events-400d-pci.json`, `ism-alerts-365d.json` — `states`/`transitions`/`ism_template`, replacing the old Elasticsearch-syntax `ilm-policies.json`); each carries an `ism_template` block so it auto-attaches by index pattern (no template setting — the dead `index.lifecycle.name` keys were removed from all 4 templates, mapping_version bumped to 2). `infra/provision.sh` now does real idempotent PUTs to `_plugins/_ism/policies/<name>` with the create-then-update-with-seq_no handshake. **Live-verified on Docker Desktop 2026-07-21**: all 4 policies installed and round-tripped via GET; a fresh `events-common-livetest-*` index auto-attached policy `events-30d` via `ism_template` (confirmed via `_plugins/_ism/explain`), proven by `services/ws3-indexer/storage/test_opensearch_live.py::_test_ism_policies_install_and_attach`, which now runs in `make test-live`. **A second, pre-existing bug surfaced by this same live run and fixed in the same pass**: `assets.json`/`events-bank.json`/`events-dc.json`/`alerts.json` each had a top-level `_comment` field OpenSearch's real `_index_template` PUT rejects (`x_content_parse_exception: unknown field [_comment]`) — silently masked forever because `infra/provision.sh`'s `curl -sf` swallows the error and prints `(skipped: ...)`, and no live run had ever inspected that output closely. Only `events-common.json` (no top-level `_comment`) had ever actually installed on a live cluster. Fixed by moving `_comment` into each template's `mappings._meta` (the schema's designated free-form field, already used for `mapping_version`) — all 5 templates now PUT cleanly, confirmed live. **Design-A follow-up (2026-07-29 audit, NOT yet live-verified — `make test-live` needs a live OpenSearch this pass didn't have access to)**: the adversarial review flagged that `events-common`'s 30-day retention was *shorter* than the 365-day alert retention it's supposed to substantiate, and that `reports-*` had no ISM policy at all. Fixed by (1) renaming `ism-events-30d.json` → `ism-events-common-90d.json` and raising its retention to 90 days (still short of full 365-day parity — that would ~12x this sector's storage cost — see the file's own description for the honest remaining gap), (2) adding `ism-reports-365d.json` (matches `ism-alerts-365d.json`'s lifetime, since a report is only as substantiable as the alerts it cites), and (3) `Rule.contributing_event_ids()` (`services/ws4-detection/engine.py`) now has `make_alert()` record ALL of a stateful rule's in-window contributing event ids (via `window.py`'s `members()`/`distinct_members()`), not just the single triggering one — previously a `common_bruteforce` alert cited "10 failed logins" but stored only 1 event id. `infra/provision.sh` and `services/ws3-indexer/storage/test_opensearch_live.py` updated to match (now 5 ISM policy files, not 4); zero-infra `make test` covers the event_ids wiring (`test_design_a_event_ids.py`) but the ISM policy install/attach itself only proves out under `make test-live`. |
| `make chaos` (M1 gate: kill each service mid-replay, assert zero lost/dup alerts) | **Proven (2026-07-18): PASS — zero lost, zero duplicate across 40 scenarios, 5 services SIGKILLed mid-replay** (see the honest scope note) | Took 4 live runs and 4 real harness bugs to get an honest verdict: (1) `docker compose kill` doesn't trigger `restart: unless-stopped` — killed workers stayed dead; (2) killer thread raced `replay()`, skipping 2 of 5 kills; (3) scenarios were laid out in FUTURE minutes (`BASE_S + i*60`), and the engine's window-poisoning guard (`_MAX_CLOCK_SKEW_MS`, 5 min) correctly fail-closed them — the deterministic 34/40 "loss" was the P0 hardening working as designed against the harness, a semantic merge incompatibility git couldn't flag; (4) all scenarios shared user "admin", so the password-spray rule legitimately fired cross-scenario and read as false "duplicates". Final run on a `down -v`-fresh stack: `scenarios=40 lost=0 duplicated=0` — at-least-once delivery + Redis-backed window counters + deterministic alert_id genuinely give effectively-once alerting through a SIGKILL of every pipeline stage. **Honest scope of that "0 lost" (2026-08-06, FIX 23): it proves CONSUMER-failure durability only.** The 40 scenarios SIGKILLed pipeline *consumers* (ws1..ws5) and replayed; they did not kill the Redis *primary* mid-write. A lost acked tail on primary FAILOVER is a different failure class that this gate cannot see: while a replica is disconnected, the primary can silently accept+ack events it has not replicated, and a subsequent promotion then never delivers them — no SIGKILL of a consumer replays that. Preventing it needs the async-replication knobs now set on the HA primary (`--min-replicas-to-write 1 --min-replicas-max-lag 10`, see infra) which trade write-availability for that guarantee. ~~Until a FAILOVER-scoped chaos scenario is added, read this row as "at-least-once through consumer kills," not "no event loss possible."~~ **The FAILOVER-scoped scenario now exists and passes (2026-08-11)** — see the row below. The consumer-kill scope note above still stands for `make chaos` itself; the two gates cover different failure classes and neither subsumes the other. **Re-verified live 2026-08-11** on the current tip: `scenarios=40 lost=0 duplicated=0`, 5 services SIGKILLed mid-replay. |
| Acked events survive a real Redis PRIMARY failover (the class `make chaos` structurally cannot see) | **Proven live (2026-08-11)** | `make ha-verify` → `tools/chaos_failover_test.py` + `tools/chaos_failover_probe.py`. Contract under test, stated precisely: **every `produce()` that RETURNED SUCCESS must be readable after the promotion**; a produce that RAISED is not covered, because refusing the write is FIX 23's guarantee working rather than a violation — so successes and refusals are tracked separately and only successes are asserted on. Live run against the HA profile: 20 baseline writes, primary `siem-bus-ha-2` (172.28.0.12) SIGKILLed mid-produce, Sentinel promoted 172.28.0.11, **22 acked / 22 present after failover / 0 lost**, with 1 write refused mid-election (`TimeoutError`) — exactly the min-replicas-to-write trade-off holding the line instead of acking an unreplicated write. Read-back uses a FRESH client against the promoted node, so it reflects that node's actual state rather than anything cached on the producer's connection. Needs the HA profile (`make ha-up`); skips cleanly otherwise. |
| Versioned OpenSearch index-template migration (`tools/migrate_opensearch.py`, M4.6) | **Proven live (2026-07-21)** | Still wire-format tested via a fake transport (`tools/test_migrate_opensearch.py`), and now additionally exercised against a real OpenSearch 2.13 cluster by `services/ws3-indexer/storage/test_opensearch_live.py::_test_migrate_live` (`make test-live`): `plan()`/`apply()` PUT all 5 templates for real, a second `plan()` reports zero drift, and `mapping_version` round-trips through the live cluster. |
| RBAC (M4.2) session store at multi-replica scale | **Redis backend proven, with signing (2026-08-06 `983efc7`)** | `services/shared/sessions.py` has `RedisSessionStore` (Redis hash per token + native `EXPIRE`) selected by `FENGARDE_SESSION_BACKEND` (`memory` default = unchanged; `redis` **requires** `FENGARDE_SESSION_SECRET` set and fails loud at construction otherwise — see `_session_secret()`/`__init__`). Since the signing pass (`983efc7`), every Redis session row is HMAC-SHA256-signed with that shared secret and `resolve()` accepts rows **only** with a valid signature (no unsigned/legacy pass-through — the review found the earlier "backward compatible no-sig" path was a forging bypass and removed it). **Live-verified 2026-08-06 against a real Redis container**: ctor refuses without the secret; an unsigned/forged row is rejected; two client instances sharing the secret agree on each other's sessions. `test_sessions.py` parametrizes both backends. A deployment changing `FENGARDE_SESSION_SECRET` invalidates all live sessions (a deliberate, documented property of signing — logins regenerate). |
| F1-F6 adversarial bug-hunt fixes (`6e3fbe4`) actually close the described holes | **Proven, zero-infra** | Each fix's regression test independently confirmed via `git apply -R`-then-run-then-restore against the fix's own diff: every test genuinely fails without its fix and passes with it restored (not just "passes today"). Full suite + ruff clean with all six applied together. Not yet run against a live Docker/Redis/OpenSearch stack — same standing zero-infra-only caveat as the rest of this table. |
| Cross-alert correlation (a low-and-slow multi-stage attacker who paces each technique under its own rule's threshold gets ONE aggregated "incident," not 27 isolated tripwires) | **Design/claim — deliberately deferred (2026-07-29 audit, Design-C)** | The adversarial design review correctly identified this as the top remaining detection-architecture gap: every rule evaluates independently against its own window, `Scorer.score()` only combines rules matching the SAME event, and nothing accumulates risk for one actor/asset across a longer horizon (hours-to-days). Building that for real — a second-pass consumer reading the `alerts` topic, a new rolling-risk window keyed by actor/asset over hours-to-days, a new "incident" alert shape, its own scoring model, dashboard surface, and test suite — is a genuine multi-day feature, not a safe same-pass fix; rushing it risks exactly the kind of new correlation-window bug (wrong horizon, unbounded state growth, false-merged incidents) this project's own fail-closed philosophy exists to avoid. **Safe scoped improvement shipped instead**: `list_alerts()` (`services/ws3-indexer/storage/{memory,opensearch}.py`, wired through `GET /api/v1/alerts?actor=&src_ip=`) now supports exact-match filtering by `actor.user.name`/`src_endpoint.ip` across tenants and the full retention window, so an analyst CAN pull every alert for one actor/IP across time today — the manual version of the query a future automated aggregator would run itself. Tested zero-infra (`test_list_alerts_correlation.py`, `test_api_v1.py::test_list_alerts_actor_src_ip_filters`); backward compatible (new kwargs, only sent when actually requested, so a pre-existing `StorageAdapter` subclass with the old 3-parameter signature is unaffected). The full aggregation engine remains open — treat it as the next roadmap item, per the review's own recommendation, ahead of adding more atomic rules. **Scoped (design only, no code) 2026-08-05**: `fengarde-sec/docs/2026-08-05-cross-alert-correlation-scope.md` — proposed shape (reuse `window.py`'s primitive with a longer horizon, a new `incidents-*` index, additive-field discipline) and the 5 failure modes any implementation must cover (false-merge on NAT/DHCP reuse, tenant isolation, unbounded state growth, chief among them). Kept out of this repo's own `docs/superpowers/specs/` per the 2026-07-31 public-doc-policy `.gitignore` rule (a forward-looking roadmap scope, not a keep-public decision-rationale doc) — same category as the HA design doc, same repo it now lives in. |
| 2026-08-06 hardening pass (PR #54) — feature/behavior claims, zero-infra-or-one-container proven | **Proven (zero-infra) + a few one-container live checks; the failover-dependent items remain CLAIM until a live kill-test** | See `implementation_report_2026-08-06.md` §Phase 5 for the review that found the regressions below and `983efc7`/PR #54 for the fixes. Concretely: **(a)** HA `BUS_BACKEND=redis-sentinel` now wires the distributed window counter (and `Sentinel.master_for()` re-resolves the current master on reconnect — ~~**code-correct, not re-proven by a live primary-kill in this pass**; the earlier live failover proof was on the bus layer only~~ **PROVEN LIVE 2026-08-11**: `make ha-verify` → `tools/sentinel_failover_live.py` drives `services/ws4-detection/test_window_sentinel_failover_live.py`, which builds the counter client exactly as `main()`'s HA branch does and holds ONE `master_for()` client across a real `docker kill` of the primary. That single-client lifetime is the point — the defect class is a client pinned to a demoted master that answers `READONLY` forever while health checks stay green, and a fresh client built after the failover would resolve the new master trivially and prove nothing. Live result: master moved 172.28.0.11 → 172.28.0.12, the long-lived client followed the promotion, and the pre-failover window state survived (count 5 → 6, asserted as a VALUE — a promoted replica that had not replicated the window would answer writes happily while having silently lost the count)); **(b)** Redis sessions are HMAC-signed and the `redis` backend fails loud without `FENGARDE_SESSION_SECRET` — **live-verified against a real Redis container** (ctor-refuses-without-secret, signed-row-resolves, unsigned-row-rejected, two-clients-share-secret); **(c)** `FENGARDE_REQUIRE_AUTH` refuses boot with an incomplete auth surface; **(d)** MFA/TOTP is opt-in per-user and login gates on `totp_code` when active — **backward-compatible when disabled**; enable/verify require the acting user's current password (reauth) — ~~**flow tested zero-infra, not a live browser/login e2e**~~ **PROVEN LIVE 2026-08-11, and it found a production defect the zero-infra tests could not see.** `services/ws3-indexer/test_mfa_live_e2e.py` drives real HTTP against the deployed handler inside the ws3 container: login → `/auth/mfa/enable` → `/auth/mfa/verify` → password-only login REFUSED → wrong code refused → password+code accepted. **The defect:** `shared/users.py` reached the TOTP primitive by walking `parent.parent / "ws6-inventory"`, which resolves in a source checkout but NOT in any deployed image — ws3-indexer's Dockerfile copies `services/shared`, never `ws6-inventory`. The import failed, the `except` silently set `_TOTP_AVAILABLE = False`, and in the shipped container `provision_totp()` raised while `verify_totp()` rejected EVERY code. MFA was **inert in production while green in CI**, because in a checkout the path resolves fine. Fixed by moving the primitive to `services/shared/mfa.py` (ws6 never imported it — its own INTERFACE.md said "hosted here, NOT wired into this service's own auth"), and the degrade is no longer silent: it now emits a `RuntimeWarning` naming the consequence. The e2e treats `_TOTP_AVAILABLE == False` as a FAILURE, never a skip, so this cannot regress quietly again; **(e)** OpenSearch write-failover round-robins across a comma-separated `OPENSEARCH_URL` and the HA profile now points ws3 at all 3 nodes — ~~**node-rotation unit-tested, node-death failover not re-run live**~~ **node-death failover PROVEN LIVE 2026-08-11**: `services/ws3-indexer/storage/test_opensearch_ha_failover_live.py` against the real 3-node HA cluster — baseline write, `docker kill siem-store-ha-1`, confirmed the node stopped answering, post-kill write succeeded via round-robin to a survivor, node restored. Run with `OPENSEARCH_URL` naming all 3 nodes (host-published as 9201/9202/9203 under the HA profile); **(f)** the E1 audit log records login/triage/report and is admin-scoped, append-only, capacity-capped, fail-open — **zero-infra tested**; **(g)** the UDP syslog deterministic_id regression was reverted (FIX 21 — window-counter member-dedup had silently under-counted N repeated identical lines); **(h)** per-source syslog `/metrics`, dashboard saved-searches/dark-mode/lifecycle, 6-parser timeutil migration, CI gates (live-Redis session+window jobs, OpenSearch CI job, devkit-feeder pip-audit/SBOM coverage, Dockerfile `pip install` fail-closed) — all zero-infra tested. Full `run_all_tests.sh` + ruff + mypy green at merge. |

## 3. Doc index — what each file is for, and its trust level

**Read `SSOT.md` (this file) first. Then only open a doc below if you need its specific detail.**

| File | Purpose | Trust level |
|---|---|---|
| `README.md` | Public-facing intro | Current |
| `CHANGELOG.md` | Version history | Current, authoritative for "what shipped when" |
| `SECURITY.md` | Threat boundary | Current |
| `CONTRIBUTING.md` | How to add a parser/rule | Current |
| `contracts/*.md` (bus-topics, ocsf-classes, sigma-convention) | Frozen Phase-0 contracts (A/B/D) | Current — these are the schema source of truth, don't restate them elsewhere |
| `docs/PHASE0_README.md`, `docs/INTERFACE.template.md` | Historical Phase-0 process docs | Historical, still accurate for what they describe |
| `docs/adding-a-parser.md` | Contributor walkthrough | Current. The line-number pointer for the parser registry (`services/ws2-normalization/parsers/__init__.py`'s `_REGISTRY` dict) was previously stale ("around line 19") and already fixed to "line 33" in both this doc and `CONTRIBUTING.md` — re-verified 2026-08-07, both still correctly say 33. |
| `docs/agent-monitoring.md` | Agent parser's 5 detection rules + `tools/agent_log_shipper.py` | **Current** (re-checked 2026-08-06) — rule names/levels match `contracts/rules/agent_*.yml`; honestly scopes "no live network listener yet." Was missing from this index entirely until now, not a sign it was stale. |
| `docs/degradation-matrix.md` | Dependency-by-dependency failure/recovery table | **Mostly current, one row dated** (re-checked 2026-08-06) — states self-current as of commit `14d00ff`. Its "no documented Redis reconnect/backoff strategy for the bus itself" row predates the 2026-08-06 hardening pass: `Sentinel.master_for()` (`services/ws4-detection/main.py`) now re-resolves the current master on reconnect, a real lever this doc doesn't reflect yet. Was also missing from this index until now. |
| `services/*/INTERFACE.md` (7 files) | Per-workstream contract | **Current (re-verified 2026-08-07)** — all 7 were re-synced and now reflect the implemented surface. `ws2-normalization/INTERFACE.md` lists all 17 shipped parsers (incl. `sysmon`, `inventory_diff`); `ws4-detection/INTERFACE.md` documents the full operator set (`in`/`contains`/`glob`/`gt`/`gte`/`lt`/`lte`/`ne`/`not_in`/`outside_hours`); `ws3-indexer/INTERFACE.md` documents `/audit` and `/auth/mfa/*`; `ws6-inventory/INTERFACE.md` lists all substantive modules (`app.py`, `authz.py`, `bus_consumer.py`, `keystore.py`, `manage_keys.py`, `mfa.py`, `store.py`) incl. tenant isolation, MFA, and the bus consumer; `ws1-collectors/`, `ws5-ai/`, `ws7-dashboard/` INTERFACEs are current. Treat every one of these as "what WS-N's *contract* is", not "what's implemented" — cross-check against the actual `services/wsN/` code. |
| `docs/superpowers/specs/2026-06-28-T3-loop-ownership-decision.md` | Bus/runner design decision | Historical, now annotated RESOLVED (was marked unproven; gaps closed since — see file header) |
| `docs/superpowers/specs/2026-07-02-fengarde-architecture-review.md` | Cross-cutting architecture review (v0.2, current) | **Current** — the most up-to-date structural analysis; adversarially reviewed, claims confirmed against code |
| `docs/superpowers/specs/2026-07-21-s7-profinet-decision-gate.md` | S7/PROFINET parser: re-investigated deferral with evidence (RFC 5424 syslog framing is public, event vocabulary is access-gated) | **Current** — the authoritative reasoning for why this parser is still deferred |
| `docs/superpowers/specs/2026-07-21-periodicity-primitive.md` | Periodicity/beaconing window primitive: design, coefficient-of-variation rationale, stated limitations | **Current** |
| `docs/superpowers/specs/2026-07-22-mitre-fire-check.md` | M7: empirical MITRE firing check design -- declared vs. fired vs. real-world-validated, kept as three separate claims | **Current** |
| `docs/superpowers/specs/2026-07-22-modbus-anomaly-scope.md` | M7: why the Modbus/TCP parser is a protocol-anomaly detector, not a vendor-log parser -- Modbus has no audit-log format to parse | **Current** |
| `docs/superpowers/specs/2026-07-21-audit-fix-plan.md` | 2026-07-21 audit: consolidated remediation log (detection-accuracy/EPS/logic findings, all fixed with re-verified evidence) | **Current** — a completed fix log with evidence, not a forward plan |
| *(moved to fengarde-sec 2026-07-31, §4)* | v0.1 build plan, v0.3 improvement plan, v0.4 build plan, B5 HA design doc, and the 3 draft `docs/posts/` write-ups | Phase-by-phase build/roadmap plans and unpublished launch-content drafts — no longer kept in this repo |
| `contracts/reporting.md` | Frozen cross-repo contract: incident-report hook request/response schema, incl. M5's additive NIS2 template-mode query params | **Current** — the seam to the paid layer's report backend |
| `contracts/triage-api.yaml` | OpenAPI 3.1 spec for WS-3's versioned REST API (M4.3) | **Current** — spec-vs-code drift is CI-tested (`test_api_v1.py`) |
| `contracts/nis2-de-schema.json` | Field schema for the NIS2/§32 BSIG report generator (M5) | **Current** |
| `contracts/tenants/README.md`, `contracts/webhooks/README.md` | Config-file conventions for per-tenant rule enablement (M4.1) and outbound webhooks (M4.4); both ship empty | **Current** |
| `docs/ops-lifecycle.md` | M4.6: schema migration, backup/restore, disk guardrails | **Current** |
| `docs/plugin-development.md` | M4.5: writing an external parser/rule-pack plugin (entry points) | **Current** |
| `docs/webhooks.md` | M4.4: configuring + verifying outbound alert webhooks | **Current** |
| `docs/nis2-report-generator.md` | M5: NIS2 report generator scope/limits, leads with the NIS2-vs-DORA caveat | **Current** |
| `docs/adr/` (6 ADRs + index) | Architecture decision records: Redis bus, OCSF, OpenSearch, microservice split, fail-closed rules, local-LLM triage | Current (M2, backfilled 2026-07-16) — each cites the code/doc proving the decision is live, not aspirational |
| `AGENTS.md` | Imported cowork stub | Minimal, ignore |

## 4. Known doc debt (don't fix silently, flag before touching)

- Two perf items from `docs/superpowers/specs/2026-07-21-audit-fix-plan.md` were
  marked "OPEN, deliberately deferred" there and never carried forward into this
  file (found in the 2026-08-13 audit — the audit-fix-plan doc itself is
  historical per §3's trust-level table, so nothing currently re-reads it; these
  two items had nowhere else to live). Still open as of this writing:
  **WS-2/scored.events double-index (P1-4 remainder)** — needs a `runner.py`
  ack-model redesign to batch safely across messages; and **XACK/XADD
  pipelining (P1-8 remainder)** — same class of ack-model contract change.
  Neither has been attempted since 2026-07-21.
- ~~Only 2 real git tags existed (`v0.2.0`, `v0.3.0`); `CHANGELOG.md` had a
  `## [0.1.0]` section with no corresponding tag, and everything since
  `v0.3.0` sat under `## [Unreleased]` because nobody re-ran the tagging
  step the v0.4 build plan itself calls for.~~ **Fixed 2026-07-23** —
  `CHANGELOG.md`'s `[Unreleased]` split into dated `[0.4.0]` (2026-07-12,
  commit `153da94` — the last commit before the ARGUS rebrand SSOT.md calls
  the "v0.4→v0.5 bridge") and `[0.5.0]` (2026-07-23, commit `c3ec328` —
  current `main`) sections; annotated `v0.4.0`/`v0.5.0` tags created and
  **pushed to `github.com/supermhel/fengarde`** (confirmed live via the
  GitHub API: `v0.4.0`→`153da94`, `v0.5.0`→`c3ec328`), with full release
  notes at `docs/releases/v0.4.0.md`/`v0.5.0.md` ready to paste into GitHub
  Releases. (An earlier attempt in a different session's environment hit an
  HTTP 403 pushing tag refs specifically — resolved this session; the 403
  was environment-specific, not a repo-side restriction.) The `v0.1.0` tag
  gap is untouched — no commit in history documents that boundary as
  cleanly as `153da94`/`c3ec328` do for v0.4/v0.5, so retroactively tagging
  it would need someone to pick a commit by judgment call, not evidence;
  left as a known gap, not guessed at.
- ~~`SECURITY.md` said "current threat boundary (v0.4)" in its header and "Out
  of scope (as of v0.4)" listed multi-tenancy and RBAC as deferred, when M4
  shipped both.~~ **Fixed 2026-07-23** — version claims genericized to
  "current" (each section already carries its own accurate milestone label),
  out-of-scope list corrected, supported-versions table updated from a
  leftover "v0.1 (`main`)" to `main`/v0.3.0.
- ~~`README.md` had a duplicated "Triage workflow" row, a stale "v0.3 shipped,
  v0.4 in progress" header, a Scorecard-badge note claiming "unknown" after
  the workflow had already run, and no row at all for M4/M5 (multi-tenancy,
  RBAC, versioned API, webhooks, plugins, chaos gate, NIS2 template).~~
  **Fixed 2026-07-23.**
- ~~`CONTRIBUTING.md`'s "Project scope" section described v0.1 ("4 parsers,
  AI triage is a stub") as the current state and pointed contributors at the
  v0.1 build plan as the scope authority.~~ **Fixed 2026-07-23** — now points
  to SSOT.md as the scope authority instead of hardcoding a version snapshot
  that goes stale on every release.
- ~~SSOT.md's own §1 header said "as of 2026-07-18" and §5 was dated `8b3f450`/
  2026-07-16 with M2 badges marked "wired, not yet fired" — both had drifted
  behind the document's own body, which already recorded CI going green
  2026-07-19 and M2 closing 2026-07-22 (PR#24). A reader trusting the section
  headers over the table rows would have under-stated progress by 5 days.~~
  **Fixed 2026-07-23** — header/§5 dates and gate statuses now match the
  latest recorded row (PR#24); parser/rule shipped-counts in §1 (15→16, 26→27)
  updated to include PR#24's Modbus addition, which had landed in its own
  dedicated row (line 48) without the summary counts being bumped to match.
- **Open thread, not yet resolved either way**: the v0.5 combined-plan doc's
  §B.4 deferred "shared pydantic schema package" to "the M2 PR that would
  introduce it" — grepped the repo 2026-07-23, zero pydantic usage anywhere.
  No PR ever made this decision either way; it's a genuinely dangling item,
  not silently dropped by this fix pass. Flag to the owner if a real answer
  is wanted.

- ~~`services/ws2-normalization/INTERFACE.md` says "3 parsers"; code has 7.~~ **Fixed 2026-07-04** — now lists all 7.
- ~~Open-core decision isn't yet reflected in `README.md`/`LICENSE`/`SECURITY.md`.~~
  **Fixed 2026-07-21** — README.md now has an "Open core" section (free forever, paid layer
  only via the `contracts/reporting.md` seam). LICENSE deliberately untouched (plain
  Apache-2.0; the split is a repo boundary, not a license term).
- **Business/launch-planning docs moved out of this repo's working tree (2026-07-21):**
  the production-roadmap-design and combined-plan specs, plus `docs/posts/launch-checklist.md`,
  `launch-drafts.md`, and `publish-pack.md` — internal roadmap, launch tactics, and marketing
  drafts don't belong in a public repo going forward. This is a going-forward split only: prior
  git history still has them (rewriting history was explicitly declined — breaks every existing
  clone/fork). This SSOT file remains the authoritative status source; nothing substantive was
  lost from what a reader needs to trust this repo's claims.
- **Near-miss of the same mistake (2026-07-29):** a sourced strategy/marketing review
  (`strategy_marketing_review.md`) was briefly committed to `docs/superpowers/specs/` and pushed
  to `main` (commit `b037962`) before being caught as the same category the 2026-07-21 move
  above exists to keep out — removed from the tree the same day. Its objective, factual
  recommendations (not the strategic analysis itself) were already applied to `docs/vs.md` and
  `docs/nis2-report-generator.md` before removal and those edits stand. The review content
  itself is still recoverable from git history (`git show b037962:docs/superpowers/specs/2026-07-29-fengarde-strategy-marketing-review.md`)
  unless/until a history rewrite purges it — that's a separate, not-yet-made decision, since
  rewriting shared history needs explicit sign-off the same way the 2026-07-21 move declined it.
- **`docs/vs.md` moved out of this repo (2026-07-31, founder-directed full-repo doc audit):**
  the competitor-comparison doc (FENGARDE vs Wazuh/Elastic Security/Security Onion + the
  German-sovereignty-player section) is competitive/positioning content, same category as the
  2026-07-21 batch and the 2026-07-29 near-miss above — recovered into `fengarde-sec`'s
  `docs/from-public-repo/specs/2026-07-31-fengarde-vs-competitors.md`, removed from this repo's
  working tree, README's link to it removed. Going-forward split only, no history rewrite. The
  same audit reviewed every other doc in this repo (specs, reviews, ADRs, posts) and judged the
  rest — including `docs/superpowers/specs/2026-07-02-fengarde-v0.3-improvement-plan.md` and
  `2026-07-10-fengarde-v0.4-build-plan.md`, which carry a short market-positioning header on top
  of otherwise-technical build plans — as legitimately public per this file's own §3 doc index;
  flagged to the founder for override rather than moved unilaterally.
- **Founder override + full public-repo doc audit, stricter rule (2026-07-31):** the founder set
  an explicit narrower bar for what stays in the public repo — only (1) contributor-need docs
  (deploy/setup/contract/API/plugin references, ADRs explaining a shipped decision's WHY,
  operational docs a self-hoster needs) and (2) trust-signal/audit-trail artifacts (the
  `review_*.md` reports, `fixes_summary*.md`, `independent_review_of_*.md` — proof-of-rigor per
  the Plan C "numbers and badges convert skeptics" strategy). This explicitly overrides the
  2026-07-31 (earlier same-day) "flagged, not moved" call above: `2026-07-02-fengarde-
  v0.3-improvement-plan.md` and `2026-07-10-fengarde-v0.4-build-plan.md` are phase-by-phase
  build/roadmap plans under the stricter rule, so they moved. Moved to `fengarde-sec`'s
  `docs/from-public-repo/specs/` (verbatim, same filenames): the v0.1 build plan, the v0.3
  improvement plan, the v0.4 build plan, and `2026-07-21-ha-design.md` (B5 central-tier HA — an
  unbuilt recommendation, not an after-the-fact decision explanation; the most ambiguous call of
  this batch, flagged as such). Moved to `docs/from-public-repo/posts/`: the three
  `docs/posts/*.md` write-ups (`ocsf-native.md`, `opensearch-not-elastic.md`,
  `local-ai-triage.md`) plus their shared `fengarde-architecture.png` diagram — all three were
  already labeled "Draft, not published" in this file's own §3 index, same category as the
  already-moved launch-checklist/launch-drafts/publish-pack. **Kept public** after the same
  judgment pass: `2026-06-28-T3-loop-ownership-decision.md`, `2026-07-02-fengarde-architecture-
  review.md`, `2026-07-21-s7-profinet-decision-gate.md`, `2026-07-21-periodicity-primitive.md`,
  `2026-07-22-mitre-fire-check.md`, `2026-07-22-modbus-anomaly-scope.md`, and
  `2026-07-21-audit-fix-plan.md` (a completed remediation log with re-verified evidence, not a
  forward plan, despite the filename) — each explains WHY a shipped decision/scope-cut was made
  or documents completed, evidenced work, not a build/roadmap plan. Links fixed in README.md,
  CONTRIBUTING.md, `docs/adr/{README,002,003,006}.md`, and this file's own §3 table (§1's
  forward-roadmap prose references left as historical text, same convention as every other
  already-shipped item in that section). Going-forward split only, no history rewrite.

## 5. M6 launch — EXECUTED 2026-07-30 (real outcome, needs periodic follow-up)

**Launched.** The owner manually posted FENGARDE across **4 communities** on 2026-07-30 and
personally answered comments/questions there. This matches the count and sequencing
`docs/posts/launch-checklist.md` (historical, moved out of this repo 2026-07-21, §4) had
planned — r/netsec → r/selfhosted → Show HN → r/blueteamsec — but that per-community mapping
is inferred from the prior plan, not independently confirmed per-post here; treat it as "4
communities, matches the planned sequence," not a verified breakdown.

**Traction log** (mechanism per `fengarde-sec/docs/2026-07-30-forward-plan-v2.md` §5.2 — a dated
table here, not a new file; cadence weekly for the first month, then monthly). Each row a real
`gh api repos/supermhel/fengarde` check, not a guess:

| Date | Stars | Forks | Open issues/PRs | Notable inbound |
|---|---|---|---|---|
| 2026-07-30 | 8 | 0 | 0 | Launch day, 4 communities posted |
| 2026-08-06 | 10 | 0 | 0 | No inbound design-partner or MSSP contact yet |

Still a point-in-time signal, not a verdict — track the trend across check-ins, don't over-read
any single row.

The gate-criteria table below records the readiness assessment that was true going into
launch (all green since 2026-07-19, per the "Bottom line" note) — kept as the historical record
of what launch was actually gated on, now superseded by the fact that launch has happened.

**Gate criteria (combined plan, M6): M1 chaos gate + M2 bench/badges + M4 MSP-grade green.**

| Gate | Status | Why |
|---|---|---|
| M4 MSP-grade | **Green** | Multi-tenancy, RBAC, versioned API, webhooks, plugins, ops lifecycle all built, tested zero-infra AND live (Track X closeout, PR#23) — the ILM/ISM policy-schema gap this row used to flag is now **fixed and live-verified** (§2). |
| M2 bench numbers + quality floor | **Green — CLOSED 2026-07-22** | Real, reproducible EPS/RSS numbers in README; ruff + coverage + **mypy now blocking** (re-measured 47 real findings, all fixed) + **mutmut baseline measured** (142 mutants on `services/shared/sessions.py`, 72% kill rate, informational) — see §1's "Forward roadmap" row for the full account. |
| M2 badges (CI/CodeQL/Scorecard) | **Green — fired 2026-07-19** | All three workflows have run on `main` and are green; Scorecard's findings drove a supply-chain pinning pass (54→19 accepted alerts, see §1's CI row). No longer "wired, not fired" — this was true as of 2026-07-18 and is now stale in the other direction; corrected here 2026-07-23. |
| M1 `make chaos` | **GREEN — PASS (2026-07-18)** | `scenarios=40 lost=0 duplicated=0`, all 5 pipeline services SIGKILLed mid-replay on a fresh live stack (Docker Desktop 4.80.0). Getting here burned down 4 real harness bugs across 4 runs (compose-kill restart suppression, killer-thread race, future-timestamped scenarios tripping the P0 window-poisoning guard, shared-username triggering the spray rule cross-scenario) — full history in §2's chaos row and `tools/chaos_test.py`'s honesty note. The interim "34/40 lost" scare was the P0 far-future guard doing its job against the harness, not a pipeline defect. |

**Bottom line: all four gate criteria were green going into launch** (M1 chaos closed
2026-07-18, M2 badges fired 2026-07-19, M2 quality floor closed 2026-07-22, M4 green since
PR#23). **M7's continuous tracks (MITRE empirical fire-check, Prometheus/Grafana
observability, Modbus OT expansion) were also substantially done as of PR#24 (2026-07-23)**,
though M7 was never a launch gate to begin with. The owner made the launch call on 2026-07-30
with all four gates green — this file recorded the facts, the owner made the decision, per
the standing division of responsibility.

M5 (NIS2 template layer) was strongly preferred in the launch narrative per the plan and is
also done (§1) — it shipped as part of what got launched.

**The launch-sequencing plan (relocated out of this repo, §4) has now been executed**, not
just written: the owner ran the r/netsec → r/selfhosted → Show HN → r/blueteamsec-shaped
posting sequence on 2026-07-30 and engaged with the resulting comments. What remains open is
ongoing traction tracking (star count, inbound interest), not the launch decision itself.

---

*Update this file, don't create a new status doc, whenever a fact in §1/§2 changes. If a
new spec doc is added under `docs/superpowers/specs/`, add one line to §3 immediately.*
