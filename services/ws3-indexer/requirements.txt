# WS-3 Indexer. Core skeleton is stdlib-only (urllib OpenSearch client). Pinned
# and actually installed by the Dockerfile (M2 supply-chain, PLAN_C Tier 2.2 --
# this used to be an unpinned inline `pip install` arg with no machine-readable
# manifest, so Dependabot/SBOM tooling had nothing to track).
redis==5.0.8       # bus backend (services/shared/bus.py::_RedisBus, lazy-imported)
