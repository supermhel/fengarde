# WS-1 Collectors. Core skeleton is stdlib-only (the contract test / zero-infra
# path needs nothing below); these are the live-stack (BUS_BACKEND=redis) extras,
# now pinned and actually installed by the Dockerfile (M2 supply-chain, PLAN_C
# Tier 2.2 -- these used to be unpinned inline `pip install` args with no
# machine-readable manifest, so Dependabot/SBOM tooling had nothing to track).
# bus backend (services/shared/bus.py::_RedisBus, lazy-imported)
redis==8.1.0 \
    --hash=sha256:a4fe1aac3d3b3cc791d4b3d5931c5a956045dc951ee74d1c913ee3ac4d2ee9fb \
    --hash=sha256:6e1a19beef9225c83efd689c7e6b7da2d5215b1f42cd13b7fc3714d0a09c7b25

# NOT installed -- real SNMP polling is unimplemented (collectors/snmp_collector.py
# reads a mock JSON source; see its docstring). Installing an unused dependency
# is dead weight and attack surface for zero benefit. Add when SNMP I/O ships
# (README's "SNMP parser: Planned").
#   pysnmp>=5.0
