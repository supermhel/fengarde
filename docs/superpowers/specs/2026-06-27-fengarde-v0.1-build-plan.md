# FENGARDE v0.1 Build Plan (harden-then-ship)

Generated by /plan-eng-review on 2026-06-27
Source design doc: `~/.gstack/projects/SIEMApp/Mel Dylan Djomou-unknown-design-20260627-232201.md` (APPROVED)
Status: **DONE (code) + PROVEN GOOD (zero-infra green + full Docker stack proven live on engine 29.6.1)** — 2026-06-30
Scope: SCOPE_REDUCED (parser coverage 4/9 in v0.1)

This plan is the output of an interactive engineering review. Every task below
derives from a specific finding, grounded in code that was read during review.

---

## STATUS — DONE + PROVEN GOOD (2026-06-28)

The v0.1 pipeline is functionally complete and **proven on the zero-infra path**.
Everything that can be verified without Docker/Redis is green; the only remaining
work is live-infrastructure *confirmation* (real Redis/OpenSearch), which this
environment cannot run (no Docker daemon, no `redis` module). Code paths for those
are in place with the exact confirmation commands named.

### Proven good — all green (run with `C:/Python313/python.exe`)

| Evidence | Command | Result |
|----------|---------|--------|
| **Acceptance test** — SSH brute-force burst → real alert in the index, idempotent under replay | `make e2e` / `python tools/demo_e2e.py` | ✅ 10/10 normalized, alert score 70, T7 dedup verified |
| **Full gate** (7 WS contracts + validator + runner + window + evaluator + both e2e) | `make test` / `run_all_tests.sh` | ✅ ALL TESTS PASS (one command) |
| Shared runner (multi-topic, ack-after-handler, DLQ, health, shutdown) | `python services/shared/test_runner.py` | ✅ PASS (Redis claim path self-skips, no broker) |
| T4 boolean evaluator (no-RCE) + T7 deterministic id | `python services/ws4-detection/test_engine_boolean.py` | ✅ PASS |
| T6 window counters (deque + Redis-ZCOUNT, both backends agree) | `python services/ws4-detection/test_window.py` | ✅ PASS |
| Pipeline composition WS-1→2→4→3 (G-01 unicode fixed) | `python tools/integration_e2e.py` | ✅ PASS |
| Structured JSON logging + trace_id | `shared/log.py` | ✅ JSON lines, trace_id propagated |

### Done (code complete + proven where provable)

- **T0** daemonize — all 5 services on the runner (`serve(...)`)/daemon; `restart: unless-stopped` on all 7 compose services. ws4/ws2/ws3(4-topic)/ws5 consume via runner; ws1 producer-daemon with health.
- **T1** Unicode crash (G-01) fixed + structured JSON logging (`shared/log.py`) replacing every runtime `print()`; `trace_id` set per message in the runner.
- **T2** `/health` — runner starts a health thread for every consumer; ws6 has its own. Exercised by the runner test.
- **T3** shared runner — built, mechanism holds on MemoryBus (see decision doc spike result).
- **T4** `eval()` → explicit boolean evaluator (P1/security; RCE surface removed) + unit test incl. a no-RCE assertion.
- **T7** deterministic `alert_id` (idempotent under replay; proven in the acceptance test) + redelivery cap→DLQ in the runner (MemoryBus-proven).
- **T6** global window count (`window.py`: deque + Redis-ZCOUNT counters) wired into WS-4; multi-replica-correct by construction, algorithm proven by the dual-backend test.
- **DX1** English README + status matrix · **DX3** preflight doctor · **DX5** CONTRIBUTING/templates/adding-a-parser + runtime stub banners (WS-5 mode, WS-2 dead-letter) · **LICENSE** Apache-2.0 · **SECURITY.md**.
- **DX2 (zero-infra)** `tools/demo_e2e.py` + `make e2e` — the acceptance feeder/proof.

### Publish prep — DONE 2026-06-28 (git)

- `git init` (branch `main`) + initial commit `8270e26` — 111 files, working tree clean.
- `.gitignore` (Python/env/data) + `.gitattributes` (forces **LF** so shell scripts work
  in Linux containers on Windows clones).
- **Secret scan** (manual grep, no gitleaks installed): clean — no `.env`, keys, or
  hardcoded creds; the only "password" hits are sample SSH log lines. Compose ships
  `DISABLE_SECURITY_PLUGIN` (no `admin:admin`).
- LICENSE (Apache-2.0), SECURITY.md, CONTRIBUTING.md committed.
- Still needs (real-world): a remote + `git push` (no `gh` / remote configured here);
  `gitleaks` pass before public push if available.

### Docker — was blocked, now FIXED (2026-06-28)

Docker Desktop 4.41.2 wouldn't start: its build component crashed on boot
(`running com.docker.build: exit status 1`), aborting the app before the engine came
up. Root-caused from `com.docker.backend.exe.log`. Fixed with a CLI backend reset:
kill Docker procs → `wsl --shutdown` → `wsl --unregister docker-desktop` (recreated
fresh; Ubuntu untouched) → relaunch. Engine up in ~20s (28.1.1, both distros Running).
`com.docker.service` had also been Stopped — started it (elevated). `vm.max_map_count`
in the WSL VM is 1048576, so the OpenSearch preflight concern is moot on Docker Desktop.

### CONFIRMED on live infrastructure (2026-06-28) — the formerly environment-gated items

- **T5** ✅ Real Redis + OpenSearch stand up via `infra/docker-compose.yml`; OpenSearch
  green; provisioning ran. The real WS-3 `OpenSearchStore` indexed an OCSF event + a
  brute-force alert and queried both back; re-indexing the alert returned `False`
  (idempotent — T7 proven on **real OpenSearch**).
- **T3/T7 Redis path** ✅ `BUS_BACKEND=redis python services/shared/test_runner.py` passes
  against a live broker — `test_redis_redelivery_and_dlq` exercises `XAUTOCLAIM` +
  `XPENDING.times_delivered` cap → DLQ. Bus produce→consume→ack verified (PEL empty
  after ack).
- **T6** ✅ `RedisWindowCounter` ZCOUNT against real Redis: count climbs 1→10, fires at
  threshold, trims stale.
- **T8** ✅ Redis-backed CI tier written: `.github/workflows/ci.yml` runs the zero-infra
  gate plus a `redis:7` service-container job running the runner test on real Redis.

### Full-stack run — surfaced + FIXED 3 container-packaging bugs (2026-06-28)

The first full `docker compose up --build` brought up all 7 services; ws2/ws4/ws6
then crash-looped — three bugs the in-memory tests can't see because they only exist
in the container layout. All three fixed (commit `d8c3f70`) and verified by mirroring
the exact container `/app` layout on the host:
- `shared/ocsf.py` found `tools/validate_contract` only via the repo layout → now also
  resolves `/app/tools` (container). [ws2]
- `ws4-detection/main.py` resolved `contracts/` as `/contracts` in-container → now finds
  `/app/contracts`. [ws4]
- `ws6-inventory/app.py` imported `shared.log`, but its image doesn't bundle `shared`
  → now logs inline. [ws6 — a regression from the DX5 logging pass]

This is exactly the value of the live run: real packaging bugs caught and fixed.

### RESOLVED — Docker Desktop upgraded 4.41.2 → 4.80.0; FULL STACK PROVEN GOOD LIVE (2026-06-30)

The 4.41.2 `com.docker.build: exit status 1` defect was fixed by upgrading Docker
Desktop to **4.80.0** (`winget upgrade Docker.DockerDesktop`), which ships engine
**29.6.1**. The engine now starts and stays up; the recurring boot crash is gone.

The last remaining confirmation is now done. On a clean stack (`docker compose down -v`
+ `up -d --build`):
- **All 7 WS services `running`, restart count 0** — no crash-looping. The three
  container-packaging fixes from d8c3f70 (ws2 `validate_contract` import, ws4 contracts
  path, ws6 no-`shared` dep) are confirmed live: ws6 logs `listening`, ws2/ws4 process.
- **End-to-end flow proven live**: 10 raw SSH `Failed password` events → `raw.events` →
  ws2 normalize → `events-common-*` in OpenSearch → ws4 detect → **1 brute-force alert
  (score 70) in `alerts-*`**. Queried directly on `http://localhost:9200`.
- **T7 idempotency proven live**: replaying the same 10 events left the brute-force alert
  count at **1** and the event count unchanged — the deterministic `alert_id` dedupes
  under at-least-once redelivery on real Redis + OpenSearch (not just the MemoryBus e2e).

**Fix applied during the live run** (`services/shared/bus.py`): a blocking
`XREADGROUP(block=5000)` was racing its own socket read-timeout and raising
`redis.exceptions.TimeoutError` every few seconds, spamming tracebacks (the runner
caught it, so the pipeline still worked, but the noise could mask real errors).
`_RedisBus.consume` now treats that timeout as an empty read and returns cleanly;
`ConnectionError` is intentionally NOT caught so genuine outages still surface. After
the fix: **0 TimeoutError / 0 Traceback lines** in the ws2/ws4 container logs.

### Remaining — needs a GitHub remote or the full UI build

- **DX4** dashboard reads live alerts (frontend wiring) · **DX2 (live)** in-stack auto-feeder
  (needs the full 7-service `docker compose up`).
- **T10** asciinema demo + **publish/push** · **T10a** `gitleaks` pass before public push —
  all need a GitHub remote (repo is local-only so far; `gh` not installed).

These are confirmation/packaging steps, not unproven design — the logic they exercise is already green on the in-memory path.

### Parallel strategic track (design only — not part of this plan's scope)

2026-07-01: a separate commercial/strategic track — **FENGARDE-Sec** (a proprietary trained
multilingual security LLM) and a **sovereign-SIEM commercial positioning** (Germany-first
banking/data-center verticals) — was documented as a design specification, then reconciled with
this repo's open-source scope via `/plan-ceo-review` the same day. See SSOT.md's open-core-split
row (§1) for the reconciled, current summary — the full design doc is the owner's internal
roadmap, not kept in this repo's working tree (see SSOT.md §4).

**Resolution: open-core.** This repo (`supermhel/fengarde`, Apache-2.0) stays exactly as-is,
permanently — the pipeline, all parsers/rules, dashboard, and StubLLM/Ollama triage are free
forever and nothing above changes. FENGARDE-Sec (the trained model + regulatory compliance layer) is
a separate proprietary product in its own private repo, plugging into this repo's existing
`services/ws5-ai/llm_adapter.py::make_llm()` seam as a third backend option alongside
`StubLLM`/`OllamaLLM`. No relicensing, no feature walk-back, no shared git history between the two.

---

## Locked decisions (from this review)

| # | Decision | Rationale |
|---|----------|-----------|
| Scope | Ship 4/9 parsers in v0.1 (Cisco ASA, AD, VMware, Linux SSH); defer SNMP/NetFlow/syslog/Win-EVT/custom-JSON to v0.2 | Only 4 parsers exist today; the other 5 are net-new (2 binary). The 4 cover the demo. |
| Issue 2 | WS-4 state → Redis sorted sets with per-key TTL + ZREMRANGEBYSCORE | Closes both leak dimensions (per-event AND per-group-key) |
| Issue 3 | Move Redis ack to AFTER processing (at-least-once) | A SIEM must not silently drop security events on crash |
| Issue 1 | Redelivery cap → dead-letter on the ack path | At-least-once without a cap = infinite poison redelivery |
| Issue 4 | Replace eval() rule evaluation with a small boolean evaluator | Explicit-over-clever; removes future injection surface |
| Test | Add a Redis-backed integration test tier to CI | Every fix above lives on the Redis path, which is untested today |
| Sequencing | Wire real Redis+OpenSearch BEFORE externalizing WS-4 state | Two-replica test must run against the final infra |

## T3 — RESOLVED (spike implemented 2026-06-28; mechanism proven on MemoryBus)

**Update:** the runner spike is built and the shared-runner direction held — no fallback
to per-service loops needed. `services/shared/runner.py` ships `serve(...)` with one
thread per topic (kills the WS-3 starvation), ack-after-handler, redelivery+cap→DLQ via
`bus.claim_pending` (XAUTOCLAIM+XPENDING), re-entry daemon loop, SIGTERM/SIGINT shutdown,
and a stdlib health thread. `bus.py` changed additively (explicit `ack`, no auto-ack,
`claim_pending`); the full contract suite (7/7) + new `test_runner.py` pass. WS-4 converted
as proof. **One thing still open: the XAUTOCLAIM/XPENDING redelivery path is written but
unrun against a live Redis** (no broker on this box) — confirm during T5. The WS-4
`uuid4()` duplicate-alert fix (T7) was intentionally left for T7, not the spike.

This unblocks T0/T2/T6/T7 — the runner they depend on now exists.

### (historical) reopened by Opus 4.8 final review

- **Direction (still defensible): a shared service runner** (`services/shared/runner.py`).
  But the decision was locked prematurely — the Opus review proved the doc was
  architecture-by-assertion. Three load-bearing claims are FALSE or unwritten against
  the actual `bus.py`:
  1. **"consume() blocks on Redis" is false.** `bus.py:70` returns on the first empty
     `xreadgroup` (`if not resp: return`). One thread over N topics STARVES topics 2..N
     under load on topic 1 — the exact problem the doc claimed to solve. The runner needs
     **N+1 threads** (per-topic + health), unmentioned. Real T0 cost, not "one-line mains."
  2. **Redelivery doesn't exist.** `bus.py:69` reads `{topic: ">"}` = new messages only.
     Unacked messages sit in the Redis PEL and are NEVER redelivered. T7's "handler throws
     → redelivered" requires `XAUTOCLAIM`/`XPENDING`; the cap count must come from
     `XPENDING.times_delivered` (survives restart). None written. Untestable on MemoryBus
     (ack is a no-op, `bus.py:46-48`), so the "validate both backends" criterion is
     Redis-only for the feature that matters.
  3. **consume()'s return-on-empty is incompatible with a daemon** — the runner must wrap
     it in its own `while not shutdown` re-entry loop; "consume() unchanged" is wrong.

- **Status: T3 must do a REAL spike that writes the actual loop BEFORE T0 lands.** Prove
  (i) N-topic non-starvation (threads or async), (ii) redelivery via `XAUTOCLAIM` +
  `XPENDING.times_delivered`, (iii) the re-entry wrapper. Then amend the T3 doc with the
  real mechanism, or fall back to per-service loops. The "collapses T0+T2+T7" claim is
  optimistic until this exists.

---

## Verified during review (not assumptions)

- **Demo data path is intact.** `linux_ssh.py:119` ("Failed password" → class 3002,
  activity_id 4, Failure) and `active_directory.py:31` (Win EventID 4625 → same) both
  emit OCSF Authentication failure events. `common_bruteforce.yml` matches exactly
  (`class_uid: 3002, activity_id: 4`, threshold 10 / 60s). The brute-force demo fires
  on SSH OR AD — both in the shipped-4. (Resolves outside-voice #4.)
- **Brute-force threshold is 10, not 5.** `test_contract.py:102` already asserts it
  fires on the 10th attempt. The design doc's earlier "5" was wrong; corrected.
- **WS-2 already has a dead-letter topic** (`main.py:39`, `raw.events.deadletter`).
  Reuse it for the new redelivery-cap path rather than building a second DLQ.

---

## Phase A — DX-first acceptance gate (autoplan final-gate decision, 2026-06-28)

The autoplan CEO review raised a User Challenge (ship the AI-triage differentiator first
as a plugin). **The user defended the pipeline-as-v0.1 direction** — recorded, not
re-litigated. The user accepted the DX resequencing: the demo is the acceptance test,
defined UP FRONT, and engineering tasks (T0/T5/T6/T7) are measured against "does the demo
still produce a REAL alert end-to-end." Rationale: both the CEO and DX reviews found the
same root flaw — the plan back-loaded everything a human experiences (English README,
LICENSE, live dashboard, demo, differentiator) behind the engineering iceberg. Flip it.

These DX tasks are now the spine of v0.1, sequenced first / alongside engineering:

- [x] **DX1 (DONE 2026-06-28)** — docs — English README + "what's real in v0.1" status matrix
  - Surfaced by: DX #4/#6 (verified) — `README.md:1` is French; WS-5 advertised with ✅ + "Ollama" implying AI exists.
  - Work: English README; status matrix at top (✅ 4 parsers + pipeline + dashboard; 🚧 5 deferred parsers + AI triage, each linking a tracking issue); relabel WS-5 as "🚧 passthrough stub (v0.2)".
  - Verify: a stranger reads top-of-README and knows in 30s what works vs. what's stubbed. Ship BEFORE any public link.

- [ ] **DX2 (P1, human ~1d / CC ~30min)** — infra — `make demo` feeder + acceptance test
  - Surfaced by: DX #2 (verified) — `restart: "no"` (`compose:55`), services drain-once-and-exit, nothing replays events.
  - Work: a `demo` compose profile / `make demo` that (after T0 daemonization) injects the 10-event brute-force burst on demand against a warm stack. This IS the asciinema demo and the v0.1 acceptance test.
  - Verify: `make demo` → a real brute-force alert appears in the dashboard, repeatably.

- [x] **DX3 (DONE 2026-06-28)** — infra — Pre-flight "doctor" (`infra/preflight.sh` + `make preflight`; checks vm.max_map_count/memory/ports with plain-English fixes)
  - Surfaced by: DX #3/#9 (verified) — OpenSearch won't boot without `vm.max_map_count`; first-run failures are stack traces, not remedies. `/health` covers running services, not won't-start.
  - Work: a preflight step (tiny container or `make` wrapper) checking `vm.max_map_count`, Docker memory (≥4GB), and port availability BEFORE OpenSearch starts, printing plain-English fixes (exact `sysctl` command). README pre-flight box with the same.
  - Verify: on a fresh Linux/WSL box with defaults, preflight prints the exact fix instead of a JVM crash.

- [ ] **DX4 (P1, human ~half day / CC ~20min)** — ws7-dashboard — Live alerts by default (PULLED FORWARD from T9)
  - Surfaced by: DX #1 (verified) — `ws7-dashboard/INTERFACE.md:5` reads MOCK data; the plan's headline success criterion is unmet by the shipped dashboard. This is the product, not "verification."
  - Work: dashboard reads live alerts from the WS-3/OpenSearch path by default in the compose profile; mock is fallback only. Merges with T9's assertion.
  - Verify: the alert from DX2's feeder shows in the dashboard alert list (not canned mock data).

- [x] **DX5 (DONE 2026-06-28)** — repo — Contributor on-ramp: CONTRIBUTING.md, .github/ISSUE_TEMPLATE/*, PULL_REQUEST_TEMPLATE.md, docs/adding-a-parser.md, LICENSE (Apache-2.0), SECURITY.md all shipped. Runtime stub banners DONE: WS-5 logs `ai triage mode = passthrough-stub` at startup; WS-2 dead-letter now reads `no parser for source_type='netflow' (v0.1 ships 4 parsers; see README ...)`. (Filing the 5 "good first issue" tickets needs a GitHub repo+remote — not a git repo yet; the `parser_request` template is pre-labeled to make it one click once published.)
  - Surfaced by: DX #7/#8 (verified) — no CONTRIBUTING/templates; "add a parser" is a clean extension point (`parsers/base.py`, `parsers/__init__.py:19`) but undocumented; the 5 deferred parsers are the obvious first PRs.
  - Work: `CONTRIBUTING.md` (sell the zero-infra `python test_contract.py` loop), `.github/ISSUE_TEMPLATE/` (bug, parser-request, feature) + PR template, 5 pre-filed "good first issue" parser tickets, `docs/adding-a-parser.md` (copy `linux_ssh.py`, show the 3 edits). Land AFTER T0/T1/T2 churn so docs aren't stale on arrival.
  - Also: runtime stub banners — WS-5 logs "passthrough stub mode (AI in v0.2: <link>)"; WS-2 dead-letter says "no parser for 'netflow' — deferred to v0.2, see #NN".
  - Verify: a newcomer can add a parser following only the doc, validated with `python test_contract.py`, no Docker.

(LICENSE/secret-scan/SECURITY.md from T10a also move EARLY — Apache-2.0 LICENSE lands before any public link, not at publish.)

## Implementation Tasks
Synthesized from this review's findings. P1 blocks ship; P2 same branch; P3 follow-up.

- [~] **T0 (IN PROGRESS 2026-06-28)** — shared + all services — Build `shared/runner.py`, rewrite mains as `serve(...)` (per T3 decision)
  - DONE: `services/shared/runner.py` built + unit-tested; `bus.py` ack/claim_pending added; **WS-4 converted** to `serve(...)` as proof. STILL TODO: convert ws1, ws2, ws3 (4-topic handler map), ws5; add `restart:` policy in compose; real-Redis confirmation.
  - Surfaced by: 2nd outside-voice — `ws1`-`ws5` are batch `run()` scripts that consume-and-exit (`ws2/main.py:49`); only `ws6/app.py:73` is a long-running daemon. Resolved by the T3 shared-runner decision.
  - Work: build `services/shared/runner.py` (`serve({topic:(group,handler)}, health_port=, max_redeliveries=, shutdown=)`) — owns the loop, acks post-handler, DLQs poison, runs health thread, exits on SIGTERM. Then rewrite ws1-ws5 `main()` as one `serve(...)` call each. Mirror `ws6/app.py`'s stdlib `ThreadingHTTPServer` for the health thread.
  - Depends on: T3 (DONE — see `2026-06-28-T3-loop-ownership-decision.md`)
  - Absorbs: the per-service half of T2 (health) and T7 (ack+DLQ) — built once in the runner.
  - Verify: each of ws1-ws5 starts, stays up, processes a stream, exits cleanly on SIGTERM; WS-3's 4-topic handler map all drain. Real-Redis confirmation per the T3 doc.
  - **Blocks: T2, T6, T7.**

- [ ] **T1 (P1, human ~2h / CC ~15min)** — ws2-normalization, tools — Fix Unicode crash + replace print() with structured JSON logging
  - Surfaced by: Step 0 + design doc G-01/G-06 — `integration_e2e.py` crashes on `→` (cp1252); `main.py:52` uses print()
  - Files: `services/shared/log.py` (new), `services/ws*/main.py`, `tools/integration_e2e.py`
  - Scope note (outside-voice #8): v0.1 = per-service local `trace_id` only. Cross-bus trace_id propagation is a v0.2 TODO, NOT this task.
  - Verify: `run_all_tests.sh` passes; no non-ASCII in print paths; logs are valid JSON with a `trace_id` field

- [ ] **T2 (P1, human ~3h / CC ~20min)** — shared — Add `shared/health.py`, wire `/health` on every service
  - Surfaced by: design doc G-07/G-10
  - Files: `services/shared/health.py` (new), each `services/ws*/main.py`
  - Depends on: T0 + T3 (a `/health` HTTP listener requires the service to be a running daemon first; `ws6/app.py` already has one to mirror)
  - Verify: `curl :PORT/health` → 200 `{"status":"ok","service":"<name>"}` for all 7

- [ ] **T3 (P1, human ~1d / CC ~30min)** — SPIKE — Decide loop ownership: who owns the long-running consume loop, bus or service?
  - Surfaced by: Issue 1 / outside-voice #7 tension, sharpened by 2nd outside voice
  - Reframe: this is not just "consume() shape" — it is the **daemon loop-ownership decision** that dictates T0's daemon shape, T2's health-thread integration, and T7's ack-after-processing placement.
  - **Written pass/fail criteria (decide the axis BEFORE coding, then the prototype reads off the answer):**
    - Choose **services-own-loop** UNLESS it forces meaningful duplicated loop boilerplate across all 7 services.
    - Choose **dual-mode `consume(once=)`** UNLESS a reviewer can construct a plausible caller that acks-before-process by accident (the footgun).
    - Whichever is chosen MUST be validated against BOTH backends — MemoryBus (where every unit test runs) and real Redis — not Redis only.
  - Files: `services/shared/bus.py`, one service loop as test subject
  - Verify: written decision doc with the axis, the chosen option, and the criterion that settled it. **Gates T0, T6, T7.**

- [ ] **T4 (P1 / SECURITY, human ~2h / CC ~20min)** — ws4-detection — Replace eval() with a boolean evaluator
  - Surfaced by: Issue 4 — `engine.py:78` `eval(...)`. **Promoted to P1/security by Opus review:** FENGARDE loads YAML rules from open-source contributors; `eval` with `{"__builtins__":{}}` is the classic insufficient sandbox = supply-chain RCE via a malicious rule `condition`. This is a security gate for an open-source SIEM, not a style cleanup.
  - Files: `services/ws4-detection/engine.py`
  - Verify: new unit test covering and/or/not/parens truth table; a malicious-condition rule cannot execute code; existing WS-4 contract test still passes (rule semantics unchanged)

- [ ] **T5 (P1, human ~2-3d / CC ~1h)** — infra — Wire real Redis + OpenSearch in docker-compose (BEFORE WS-4 externalization)
  - Surfaced by: outside-voice #2 (sequencing) + #5 (OpenSearch is multi-day)
  - Files: `infra/docker-compose.yml`, OpenSearch index templates/mappings for OCSF fields
  - Gotchas (outside-voice #5): OpenSearch needs OCSF index mappings, JVM heap config, and `vm.max_map_count` tuning (fails to start on default Linux/WSL). Budget real time here.
  - Verify: `docker-compose up` → all services healthy; an OCSF event indexes and is queryable

- [~] **T6 (CODE-COMPLETE + ALGORITHM-PROVEN 2026-06-28)** — ws4-detection — Global window count for multi-replica correctness
  - Surfaced by: Issue 2 / Opus review — the threshold count was a local deque, so two replicas would split it and the brute-force alert would never fire.
  - DONE: extracted the window into `services/ws4-detection/window.py` — `DequeWindowCounter` (single-replica/tests, unchanged behavior) and `RedisWindowCounter` (ZADD + ZREMRANGEBYSCORE + ZCARD + EXPIRE per `(rule,group)`, member = OCSF ingest_id so it can't double-count). `engine.py` uses the counter via `Rule.set_counter`; `ws4/main.py` wires `RedisWindowCounter` when `BUS_BACKEND=redis`.
  - PROVEN: `services/ws4-detection/test_window.py` drives BOTH backends through a fake sorted set — counts climb 1..10 at the threshold, stale events age out, same-member doesn't double-count, and the two backends agree. Deque path still green in the WS-4 contract + acceptance e2e.
  - REMAINING (live only): a real two-replica smoke test on compose Redis (needs a broker). The atomic ZCARD makes it correct by construction; this is confirmation, not design.

- [ ] **T7 (P1, human ~1-1.5d / CC ~45min)** — shared — At-least-once: real redelivery + cap → DLQ + idempotent producers
  - Surfaced by: Issue 3 + outside-voice #1, MATERIALLY EXPANDED by Opus review
  - Files: `services/shared/bus.py`, `services/ws4-detection/main.py`
  - Real mechanism (not the hand-wave): redelivery does NOT exist today — `bus.py:69` reads `{">"}` (new only). Add a PEL path: on each cycle `XAUTOCLAIM` (or read `id="0"`) the pending entries, get `times_delivered` from `XPENDING`, DLQ when `>= max_redeliveries`. The count MUST come from Redis PEL, not an in-memory counter (lost on restart).
  - **Idempotent producers (Opus review):** at-least-once + non-idempotent produce = duplicate alerts. `ws4/main.py:44` uses `uuid.uuid4()` per alert → redelivery makes a NEW alert_id → undeduplicatable duplicate (corrupts the demo's headline count). Replace with a DETERMINISTIC alert_id: `hash(rule_id + group_key + window_bucket)`. WS-3 index is already idempotent (router doc_id); WS-5 alert_id is already stable (`ai-{event_id}`) — only WS-4 needs the fix.
  - Verify: handler throws → message redelivered via PEL (not lost); after N → `*.deadletter`, consumer continues; redelivery of a WS-4 alert produces the SAME alert_id (no duplicate doc); cap survives a consumer restart

- [ ] **T8 (P1, human ~1d / CC ~30min)** — CI — Redis-backed integration test tier
  - Surfaced by: Test review — all current tests run on MemoryBus; the Redis path is untested
  - Files: `.github/workflows/ci.yml` (new), a marked integration suite
  - Verify: GitHub Actions spins a Redis service container; consume-contract, ack-redelivery, and WS-4 TTL tests run against real Redis. Memory suite stays the fast unit gate.

- [ ] **T9 (P1, human ~half day / CC ~20min)** — ws2/e2e — Assert 4/9 normalize + brute-force alert visible in WS-7 dashboard
  - Surfaced by: design doc success criteria + outside-voice #4 (verified path)
  - Files: `tools/integration_e2e.py`, WS-7 alert-list check
  - Verify: WS-1→2→4→5stub→3 for the 4 parsers; brute-force alert reaches `triaged.alerts`, is indexed, AND shows in the dashboard alert list
  - ADDED (2nd outside voice): explicit window assertion — 10 auth-failures within 60s FIRE, the same 10 spread beyond 60s do NOT. Catches the time-units seam (`engine.py:92` assumes epoch-ms; nothing enforces it). Also assert `src_endpoint.ip` is populated for BOTH SSH and AD failures (AD 4625 without IpAddress would collapse all attackers into the `"None"` group bucket — `engine.py:88`).

- [ ] **T10 (P1, human ~2-3d / CC ~1h)** — docs/release — README + architecture diagram + asciinema demo + publish
  - Surfaced by: design doc distribution plan + outside-voice #10 (positioning)
  - Files: `README.md`, `docs/architecture.md`
  - Positioning rule (outside-voice #10): README states plainly that v0.1 is "a working SIEM pipeline; local AI triage lands in v0.2." Do NOT imply AI triage exists. WS-5 is a documented passthrough stub.
  - Verify: a stranger can `docker-compose up` and reach a dashboard alert following only the README

- [ ] **T10a (P1, human ~half day / CC ~20min)** — release-hygiene — LICENSE + secret scan + SECURITY.md (BLOCKS publish; 2nd outside voice)
  - Surfaced by: 2nd outside-voice — "open source" with no LICENSE = legally all-rights-reserved (nobody can use it); a security tool with no disclosure policy invites public zero-day.
  - Work: (1) add **Apache-2.0** LICENSE (chosen — permissive + patent grant, enables later dual-license/open-core); add SPDX headers; (2) run `gitleaks`/`trufflehog` over the full history BEFORE first public push — T5's docker-compose will add OpenSearch creds (default `admin:admin`); add `.gitignore` + `.env.example`, never commit `.env`; (3) add `SECURITY.md` with a disclosure contact; (4) add `CONTRIBUTING.md` stating "v0.1 = SIEM pipeline, AI is v0.2" so incoming issues are triaged against real scope.
  - Verify: secret scan clean on full history; LICENSE present; `SECURITY.md` reachable from README
  - **This task gates the public push in T10.**
  - PRODUCT threat model (Opus review — the SIEM's OWN security, distinct from repo hygiene): document and decide for v0.1: (a) no service has authn — `ws6/app.py:70` binds `0.0.0.0` open, and the new `/health` threads bind openly; at minimum bind to localhost/compose-network only and state "not internet-exposed in v0.1"; (b) the demo must NOT ship enabled default OpenSearch creds (`admin:admin`) reachable from outside the compose network; (c) cross-reference the T4 eval-RCE fix here — a security tool that executes contributor rule files is the product's biggest self-inflicted risk. If full authn is out of scope for v0.1, say so explicitly in SECURITY.md with the threat boundary.

---

## NOT in scope (explicitly deferred)

- **5 parsers** (generic syslog, SNMP, NetFlow, Windows EVT, custom JSON) — net-new, 2 binary; v0.2.
- **AI/LLM triage** (Ollama+Qwen), prompt-injection guardrails, AI observability — v0.2. WS-5 is a passthrough stub in v0.1.
- **Fleet observability, k8s/Helm, Tauri desktop shell, endpoint agents (WS-8)** — v0.2/v0.3.
- **Cross-bus trace_id propagation** — v0.1 ships per-service local trace_id only (TODO below).
- **Full reliability suite** (retry/backoff beyond the redelivery cap, idempotency keys) — v0.2.
- **Load testing / data retention ISM policy** — v0.2 (see TODOs).

## What already exists (reuse, don't rebuild)

- `services/shared/bus.py` — bus abstraction with both backends. Modify, don't replace.
- `services/ws2-normalization/main.py:39` — dead-letter topic pattern. Reuse for T7.
- `services/ws4-detection/engine.py` — stateful windowing logic is correct; only the
  storage backend and the eval() call change.
- `contracts/rules/common_bruteforce.yml` — the demo rule. Already source-agnostic
  (fires on AD/LDAP/RADIUS/SSH). No new rule needed for the demo.
- `run_all_tests.sh` + per-WS `test_contract.py` — the existing memory-bus gate. Keep
  as the fast unit tier; the new Redis tier (T8) is additive.

## Failure modes (new codepaths)

| Codepath | Realistic failure | Test? | Error handling? | Silent? |
|----------|-------------------|-------|-----------------|---------|
| Redis ack-after-processing | handler throws mid-process | T7 | redelivery cap → DLQ | No (DLQ + log) |
| WS-4 Redis sorted sets | quiet source IP never expires | T6 regression | per-key EXPIRE | No (bounded) |
| Boolean evaluator | semantic drift vs old eval() | T4 | rule misfire | **Was silent — T4 test closes it** |
| docker-compose OpenSearch | `vm.max_map_count` unset → won't boot | manual (T5) | startup fails loud | No (loud) |

## Worktree parallelization

| Lane | Tasks | Notes |
|------|-------|-------|
| Pre | T3 (spike) | loop-ownership decision; gates T0/T2/T6/T7. Do this FIRST. |
| A | T1 → T0 → T2 | T1 (logging, every main.py) → T0 (daemon conversion, every main.py) → T2 (health, every main.py). STRICTLY sequential — all three edit every service's `main()`. |
| B | T4 | ws4-detection eval swap — **must finish and merge BEFORE T6** (both edit `engine.py`) |
| C | T5 → T6 → T7 → T8 | Redis critical path. T6 needs T0 (daemons) + B merged (engine.py). T7 needs T0. |
| D | T9 → T10a → T10 | T9 may loop back into T5 (missing OCSF index mappings). T10a gates the T10 publish. |

Execution: **T3 first (alone).** Then launch lane A and lane B in parallel. Lane C starts after T3 + T0 land and B has merged. Lane D last.

Conflict flags (corrected by 2nd outside voice):
- **T4 and T6 both edit `services/ws4-detection/engine.py`** — were wrongly in parallel lanes. Now serialized: B (T4) merges before C reaches T6.
- **T1, T0, T2 all edit every `services/ws*/main.py`** — the real collision (not the `shared/` new files). Serialized in lane A.

## Timeline realism (outside-voice #9)

The design doc says 4-6 weeks. After the 2nd outside voice surfaced the daemon-conversion
iceberg (T0), the honest estimate is **8-10 weeks solo**. The plan is almost entirely
integration work (Redis + OpenSearch + end-to-end), the T3 spike is a hard dependency,
T5 (OpenSearch) is multi-day, AND converting 5 batch scripts into daemons (T0) is a
cross-cutting refactor that gates T2/T6/T7. The single most likely estimate-killer is
the **T3 runner mechanism** (per-topic threads + Redis PEL redelivery), which gates
the entire P1 path and is harder than "build the runner once" implied — the Opus review
showed it's unwritten and the easy framing was false. T5 (OpenSearch) is a known unknown
with a budget; the runner loop is the real iceberg under the iceberg. Treat "4-6" as
fiction; plan for 8-10+ and do the runner spike before committing to any date.

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` (via /autoplan) | Scope & strategy | 1 | ISSUES_OPEN | Strategy User Challenge (pivot to AI-triage-plugin); user defended pipeline-as-v0.1, accepted DX resequencing |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | unavailable | Codex not authenticated; all autoplan voices ran Claude-subagent only |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | ISSUES_OPEN | 6 issues + 3 outside-voice passes (incl. Opus 4.8); T3 reopened |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | n/a | No new UI in v0.1; folded into DX |
| DX Review | `/plan-devex-review` (via /autoplan) | Developer experience gaps | 1 | ISSUES_OPEN | TTHW friction 8/10→3/10; 8 findings (mock dashboard, French README, no demo feeder, vm.max_map_count, no LICENSE/CONTRIBUTING) → DX1-DX5 added |

- **CROSS-MODEL:** Eng review + 3 outside-voice passes (2× Claude, 1× Opus 4.8 max). 1st added sequencing/redelivery/OpenSearch effort. 2nd added the daemon iceberg (T0) + the T4/T6 lane conflict + LICENSE/secret/SECURITY (T10a). 3rd (Opus 4.8) was decisive: it proved the T3 shared-runner decision was architecture-by-assertion — `consume()` doesn't block (`bus.py:70`), redelivery doesn't exist (`{">"}`, `bus.py:69`), WS-4 alerts duplicate under at-least-once (`uuid4()`, `ws4:44`), and the multi-replica threshold check breaks the demo (local deque, must move to Redis ZCOUNT). Promoted T4 to P1/security (eval = RCE for community rules), expanded T7 (real PEL redelivery + deterministic alert_id), amended T6 (atomic count), added product threat model to T10a.
- **CEO+DX (via /autoplan):** Both reviews found the same root flaw from opposite ends — v0.1 back-loaded everything a human experiences (differentiator, English README, live dashboard, demo, LICENSE) behind the engineering iceberg. CEO recommended pivoting to an AI-triage plugin; **user defended pipeline-as-v0.1 at the final gate** and accepted the DX resequencing (demo as up-front acceptance test; tasks DX1-DX5 added; LICENSE + live dashboard pulled forward).
- **VERDICT:** ENG + CEO + DX REVIEWS COMPLETE → **IMPLEMENTED + PROVEN GOOD (zero-infra)**. T3 runner built and resolved (mechanism holds on MemoryBus). T0/T1/T2/T4/T7 + DX1/DX3/DX5 + LICENSE done and green. v0.1 acceptance test (`make e2e`) proves SSH brute-force → real idempotent alert end-to-end with no infra. Remaining items are live-infra confirmation only (Docker/Redis), see STATUS block at top.

NO UNRESOLVED DECISIONS
- T3 runner mechanism — RESOLVED: shared runner implemented (`services/shared/runner.py`), proven on MemoryBus; per-topic threads, ack-after-handler, redelivery+cap→DLQ, re-entry, health, shutdown. Live-Redis `XAUTOCLAIM` path code-complete, confirmation pending a broker (T5).
